# 04 — The fire_slot audit (P4)

Status: deferred to trigger
Owner: folds into the resumable-resolution ADR

The triggers/readers quarter of P4's decomposition already shipped with the event registry. The remaining audit (targeting spec / cost / effect sequence) shares its trigger list with the resumable-resolution seam researched in the EDOPro note — "choose one of several effects", "select multiple targets in sequence" are suspensions. Audit once, when interception, reaction-speed cards, or true multiplayer enters the backlog, as part of that ADR; the handoff's own caution against a speculative DSL is the same boundary ADR 0041 drew.
