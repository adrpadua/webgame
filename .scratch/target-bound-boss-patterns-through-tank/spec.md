# Target-Bound Boss Patterns Through Tank

Status: resolved

## Closure

Design vocabulary and a future-only Furnace Cleave example, Architecture's no-live-content `BoardQuery` seam, and Test Automation's independent semantic matrix agree. A future encounter may consume the seam only through a separately approved authored-content delivery.

## Approved outcome

Create a reusable, future-party Boss capability: select a Tank, derive a legal hex-edge Facing from Boss to selected Tank, include the Tank's hex when authored, and project a cleave/cone through and beyond the Tank with off-board clipping. It is not a live Embermaw change or player-card targeting UI.

## Confirmed delivery decision

On 2026-08-14, the user approved documentation plus authoritative resolver/probe support behind no live encounter content. This remains a future-party capability: it does not authorize a default Embermaw edit, player-targeting UI, or facing-system expansion.

## Sequence if resolver support is approved

1. Design records canonical Party-play vocabulary, three-category distinction, and one future human-readable example.
2. Architecture deepens the existing BoardQuery/target-selector geometry seam; no default encounter resource changes.
3. Test Automation independently verifies source-to-target snap, Tank inclusion, continuation, clipping, and selected-target-versus-affected distinction.
4. UI/UX remains a later telegraph consumer only.

## Non-goals

No Embermaw retune, Raking Claw/Cinder Breath change, player targeting UI, arbitrary angle, persistent player-facing system, or general pattern migration.

## Return protocol

The mandatory return packet in [the recovery kit](../../docs/agents/recovery-kit.md#mandatory-return-packet) applies to every assigned owner.
