# Use seeded randomness for replayable Encounters

Rules-relevant random choices in an Encounter use an injected, seeded random source. This replaces direct engine shuffles so an Encounter Resource and legal action sequence reproduce the same rules state; presentation-only variation remains outside this contract.
