# Design Decision Log

This log records current design-team decisions and recommendations. It does not replace ADRs; promote a decision to an ADR when it creates a long-lived architectural or product constraint.

**Adding a row: put `D-NEW` in the ID column and do not pick a number.** Run `cd web && npm run log:ids -- --fix` and the number is assigned against `origin/main` as it stands at that moment, and carried into any file on your branch that cites it. Picking a number by hand is what produced two D-045s, two D-046s, two D-047s and two D-057s: by the time a branch merges, the number it read is often gone. `npm run verify:local` refuses to pass while a row is unnumbered or holds a number `origin/main` has since taken.

| Date | ID | Status | Decision | Rationale | Evidence needed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-13 | D-001 | Adopted | `EncounterEngine` is the sole rules authority for content behavior. | Content text cannot claim an effect the engine does not resolve. | Existing SDK, content, and parity probes stay green. |
| 2026-08-13 | D-002 | Adopted | The current Embermaw slice is an eight-Round solo tank teaching encounter, not a complete raid proof. | It correctly proves Action Bar, facing, telegraphs, hazards, and card-fueled movement; it does not prove party roles. | New-player walkthrough and nine-probe suite. |
| 2026-08-13 | D-003 | Proposed | No Boss counter tag may promise `Kill Adds` unless the active encounter deck contains an executable Minion answer. | A visible callout must name an available response. | `whelp_clear` probe and player test. |
| 2026-08-13 | D-004 | Superseded | Do not expand the default two-card deck until playtests confirm useful Slot Tension; use the five-identity list only as the first controlled test deck. | More identities can conceal a weak core loop. Superseded by the user-approved default-deck migration recorded in [issue 04](../../.scratch/product-backlog/issues/04-promote-aegis-starter-deck-to-shield-wall-kit.md): the five-identity list is now the live/default deck as a product exception, without claiming the playtest evidence this decision originally required. | Structured eight-Round playtests remain owed as follow-up evidence for the now-live list. |
| 2026-08-13 | D-005 | Proposed | Treat current Scorched as route blocking in player-facing copy until forced movement exists. | Its entry damage is not reachable through the current voluntary movement path. | Hazard behavior probe and future forced-movement probe. |
| 2026-08-13 | D-006 | Adopted | Build Whelp intent before Phase II. Executed 2026-08-17: at each Round's end step a Whelp advances one hex toward its nearest Hero, or bites for `1` once adjacent — the creep is the deadline. Bites are Raid Hits; the bite is adjacency-gated because the evaluation sweep showed an unconditional same-round bite made the solo Round-4 checkpoint unreachable in principle. | Adds need a visible deadline before Phase II asks players to combine spatial problems. | Engine tests cover advance, bite, cleared-Whelp, and no-Riposte paths; the sweep holds both D-016 walls with the checkpoint restored; `minionIntents` is the visible projection awaiting board UI wiring. |
| 2026-08-13 | D-007 | Adopted | Elian Voss is a Shield Wall Tank. | MMO players should immediately recognize a defender who holds the dangerous space and protects the party. | Future deck playtests must show mitigation and positioning are its dominant decisions. |
| 2026-08-13 | D-008 | Adopted | Armor is a pure damage shield for Elian Voss. | Armor remains an easy-to-read defense layer. It does not become a spendable conversion resource. | Tank-card probes must verify that Armor absorbs damage before Health loss. |
| 2026-08-13 | D-009 | Deferred | Interception is Elian Voss's default ally-protection tool. | It creates a clear emergency-save moment and rewards placement without requiring allies to remain adjacent, but ally targeting and redirection are not yet under shared contract. | A future engine and UI extension must prove selected-ally damage redirection before the card is added to the live deck. |
| 2026-08-13 | D-010 | Deferred | An Interception redirects one next damage event and then expires. | A single reliable save protects a party member without canceling a full sequence of raid pressure, but the timing and expiry behavior still need executable multi-Hero coverage. | An interception probe must cover consumed and unused expiry paths. |
| 2026-08-13 | D-011 | Deferred | Elian Voss protects by holding the Guarded Front, building Armor, and using short-response Interception. | Guarded Front and Armor are already core to the fantasy, but the full three-part package remains deferred until Interception is implemented and validated. | A character playtest must show each action has a distinct use. |
| 2026-08-13 | D-012 | Adopted | Elian Voss is Captain Elian Voss, The Last Gate. | A grounded, disciplined defender gives the Shield Wall a distinct identity without borrowing the Paladin's holy spectacle or a battle captain's support language. | Future card text, art, and voice lines follow the Hold, Brace, Cover, Clear, Advance language. |
| 2026-08-13 | D-013 | Adopted | Whelp Clear and Slow Top Card acceptance remain isolated replay contracts until their probes pass. | The first deck expansion must prove a targetable Minion answer and standard full-charge cleanup without altering the live deck. | Seeded `whelp_clear` and `slow_top_card_cleanup` scenarios plus scene parity. |
| 2026-08-16 | D-014 | Proposed | Kessa Varn, The First Step, is the Vanguard-pattern second tank: Momentum tempo, Boss facing manipulation, and thin standing mitigation as the stated price. Design in [kessa-varn-design.md](heroes/kessa-varn-design.md). | The Second Hero Of A Role rule requires a distinct strength-and-price pattern; Elian holds the Warden pattern, so the second tank claims initiation and tempo. | Momentum and facing-turn engine seams, focused probes named in the design doc, and three new-player sessions. No runnable resources before the seams exist. |
| 2026-08-17 | D-015 | Superseded | Riposte Ready is cashed by any Boss-damage card for `+1`; a legal Shield Slam takes the full `+2`. Cards without Boss damage never consume it. User-approved; options and rationale in [riposte-payoff-deepening.md](design-proposals/riposte-payoff-deepening.md). Superseded by D-064 with its diagnosis intact: an earned proc that silently evaporates is the worst payoff failure mode, and the Signature Slot's banked Charge cures evaporation at the root where this graded cash-out only priced it. The graded rule stays live in the engine until the migration increment ships. | Controlled-cohort evidence showed the earned window converting in only one of three seeds because Shield Slam (2 of 20 cards) was its only spender; an earned proc that silently evaporates is the worst payoff failure mode. The graded cash-out keeps Shield Slam the ceiling while making the floor never-zero and adding spend-order mastery. | Riposte probe off-payoff coverage, web engine tests, and a fresh evaluation cohort under the new rule — prior cohorts are historical evidence for the old rule only. |
| 2026-08-17 | D-016 | Adopted | The raid is a team game: a solo Hero defeating a Boss is a tuning defect signal, never a success criterion. Evaluation cohorts record any solo Boss kill as a red-flag finding. | User-stated design principle. The solo slice tests role execution to the Round-4 checkpoint; Boss defeat belongs to a Party. An economy or deck that solo-kills is over-rich or under-pressured by definition. | The deck-evaluation rubric's solo-slice boundary carries the rule; future Party-scale criteria supersede the solo framing, not the principle. |
| 2026-08-17 | D-017 | Amended by D-062 | Counter-pressure against Boss-range camping ships as authored content: Raking Claw carries a `+3` `unguarded_bonus` when no Hero holds the Guarded Front at resolution, and Embermaw's solo-slice health rises from `36` to `48` so the solo damage race also fails. `boss_damage` stays positionless per the 2026-08-16 ruling. | User directed the fix; issue 15 left the mechanism open. The unguarded bonus makes holding the Guarded Front the cheapest place to take the unavoidable hit (Warden identity), and the health raise closes the second dominant line the red-flag search found (a dodge-positioned double-attack racer). Both Tank Design Principle walls now hold: the 12-policy sweep shows 0% solo victory and 0% enrage survival, and the committed `embermaw_solo_ceiling` Scenario dies in Round 6 with Embermaw at 10 health. | Sweep metrics stay at 0% victory; the Scenario generator exits red-flag on any found solo victory; `boss_beats`, `content`, `parity`, `records`, and riposte probes cover the new field and values. |
| 2026-08-17 | D-018 | Adopted | The user reaffirmed ADR 0019: all rules, content, and UI work happens in the web Encounter Engine/Workbench only. The Godot codebase, `.tres` resources, and probe suite are frozen reference material; the D-015/D-017 Godot-side syncs were the last, kept as pushed rather than reverted. New-work gates are `npm test`, `npm run evaluate`, and the Scenario generator's D-016 red flag. `data/*.json` is the authored content source of truth (ADR 0020). | Dual maintenance made every rules change land twice and risks divergence; ADR 0019 already decided the platform and the merge-era work under-weighted it. | Live rules/content docs no longer point work at Godot artifacts; the probe-harness doc carries the freeze banner. |
| 2026-08-17 | D-019 | Adopted | Fortify grants a `Fortified` commitment that lands its 6 Armor at the NEXT Round start, after the Armor wipe. Card text: `Gain 6 Armor at the start of the next Round.` | Under the old rule, Slow-window Armor was wiped before any damage could reach it — Fortify was a functionally dead card in the live deck (the first-turn doc had already worked around it). The delayed grant realizes the card's stated intent (prepare for a future threat) and gives the Slow Window a unique job: Fortify is now the only card that can pre-block the next Round's Instant Row, including earning Riposte Ready from a fully blocked Instant Tank Hit. | Engine test covers the full arc: no immediate Armor, grant after the wipe, Instant-row block at zero Health loss, normal expiry a Round later. Deck-evaluation freshness: prior cohorts are historical evidence for the old rule. |
| 2026-08-17 | D-020 | Adopted | The boss-design direction is canon, documented as the [Encounter Design Bible](../rules/encounter-design-bible.md): the Boss is a generator of shared escalating problems; the Timeline is a core system, not UI; Tank/Healer/Damage are three questions about the same problem ("Tank owns mitigation, Healer owns sustain"); cooperation is preferentially structural ("no stat total occupies two hexes") over role-locked; escalation grows demand density, not just numbers. | User-supplied design handoff, converging with the tank/healer research and D-016/D-017. Refines the Role-Load-Bearing Beats rule: structurally-suited problems beat arbitrary role locks. | Party-scale playtests use the north-star question: can each player name their part and someone else's in the same sentence about the outcome? |
| 2026-08-17 | D-021 | Superseded by D-053 | **The `Forecast Row` half is removed (ADR 0031, 2026-08-18) after failing the acceptance gate this decision set for it; the two-horizon Timeline and the Consequence Tier survive, the latter now gating where a `Severe` Beat may *open* rather than which horizon it must reach.** Timeline disclosure is staged across three horizons ([ADR 0026](../adr/0026-disclose-boss-beats-in-stages-across-three-timeline-horizons.md)). A new `Forecast Row` previews next Round's whole Boss Program at family level; `Incoming` and `Instant` keep positional meaning and carry complete parameters. Telegraph Proportionality becomes a three-tier ladder — `Chip` anywhere, `Structural` no later than Incoming, `Severe` in Forecast first — with the old justification escape hatch removed. The Briefing is the catalog; the Forecast Row is the schedule. | Our two rows both sit inside one Round (ADR 0024), so the research's Forecast/Imminent pipeline cannot be obtained by renaming them. Reserving resources for a *category* of problem is what makes a Tank's Guard budget and a Healer's pre-coverage real decisions. The row and the tier ladder are one decision because the `Severe` tier has no mechanism without the row. | Timeline conversion rate over a Party's first three attempts (D-027). Shipped 2026-08-17: `forecast()` is a pure projection and the strip renders it as the faintest row; `consequence_tier` is authored on every Beat and the ladder's implications are enforced as tests over live content. Round `1` is deliberately not forecast — nothing earlier could have shown it — so the ladder's one exception is that a first program may carry no `Severe` Beat. Embermaw has no `Severe` Beat at all yet, so that tier is enforced but unexercised. Accepted openly: the row is uninformative to a veteran while Embermaw's rotation loops deterministically — that is the standing argument *for* the D-024 Module Slot, not against the row. |
| 2026-08-17 | D-022 | Adopted | No rules-relevant random choice may resolve after the player window that answers it ([ADR 0025](../adr/0025-randomize-before-the-window-that-answers-it.md)). | ADR 0012 made randomness reproducible but never said *when* it may resolve, so a post-commitment roll would satisfy it while producing the research's named unfair-feeling failure. Free to adopt now: the authored encounter resolves with no rules randomness at all, so the law forbids nothing today and constrains everything after. | A content-schema check that no authored random draw is scheduled after its answering window; failure attribution as the play-feel signal (D-027). |
| 2026-08-17 | D-023 | Adopted | Escalation is the encounter's only clock ([ADR 0027](../adr/0027-make-escalation-the-encounters-single-clock.md)): one fixed `0`–`5` scale on every Boss, `+1` automatically each Round end, plus authored per-Beat acceleration for unanswered demands, with the top threshold as the hard wipe. `End-of-Clock Behavior` becomes that threshold's effect; `Enrage Timer` retires. Adopted in the solo Embermaw slice now. | A binary Round limit is a clock the party reads but never feels, and gives Damage nothing structural to own. More importantly it converts D-016's stalemate wall from a health value that better play erodes into an economic impossibility optimization cannot repair. The fixed scale is a literacy requirement; per-Beat acceleration keeps Boss identity from flattening into one attrition formula. Shipped 2026-08-17 with two narrowings found in build: automatic ticks start at `roundLimit - 4` so ticks alone reach the top exactly at the Encounter Clock (a 0–5 scale ticking from Round 1 would be a five-Round clock), and acceleration ignores a Minion that arrived this Round because it spawns in the Incoming Row where no player window can reach it. | Sweep and Scenarios regenerated; prior cohorts are historical evidence for the old rule. `victory%` stays `0%` and `checkpoint%` is identical to the pre-change baseline on all twelve existing policies — the late start's purpose. The enrage wall is measured, not asserted, by a standing criterion rather than a percentage: at least one policy reaches the Encounter Clock and dies to Escalation at `0.00` Boss damage, which is what the survival-biased `turtle` policy was added to produce. Figures are dated observations — see ADR 0027's 2026-08-17 correction for why the originally recorded percentages went stale. Embermaw's Brood Call penalty is authored at `0` until a Whelp answer exists (D-003); the mechanism is covered against a priced catalog variant. |
| 2026-08-17 | D-024 | Adopted | Encounters are authored in three layers: **Boss Core** (the learnable Boss Program spine), **Encounter Module** (one `Module Slot` per Program, filled by one of several authored Beat groups, chosen by a printed shareable `Raid Seed`), and **Difficulty Layer** (must change a mechanic or requirement; numbers alone are not a layer). `Practice Mode` is deliberately not a Difficulty Layer: economic tolerance only, never a softened failure condition. Vocabulary adopted now; no modules authored until the Party model exists. | Pure scripts go rote and fully shuffled decks stop mastery transferring; a bounded slot keeps both. Slot granularity beats per-Beat parameter randomization, which is the shape that tempts an author into violating D-022, and keeps validation to N combinations. Excluding Practice Mode from the layer vocabulary is what stops "easier numbers" re-entering through the door the layer test closes. Rejected 2026-08-17: revision 2's content-source split of the Timeline rows (boss choreography above encounter pressure). The valuable half of it is a stability contract, which Boss Core and Module Slot already hold on the better axis — authoring stability rather than screen position — and the split does not survive contact with our content, since choreography and pressure share one authored program today. | First Party session. Modules are validated in combination, never independently — module overlap, not individual values, is where difficulty comes from. |
| 2026-08-17 | D-025 | Adopted | Role identity is superiority, not exclusivity. A problem is **run-ending** if one unanswered Round causes unavoidable defeat, permanent Hero loss, or an Escalation Threshold crossing; run-ending problems must carry at least one off-role answer at a premium, and elsewhere it is optional. Off-role costs draw from a fixed vocabulary: extra actions, a card discard, a Class Resource, or accepting partial damage. | Hard locks turn an encounter into matching coloured keys to coloured locks and let one exhausted player convert every mistake into deterministic failure. Requiring off-role lines everywhere would triple authoring cost on Beats where nobody would pay the premium, so the requirement is scoped to where a missing specialist would otherwise make failure automatic. A fixed cost vocabulary is learnable; one standardized currency is not universally payable. | First Party session, with the no-healer-clear test and role counterfactual value (D-027) as the reads. |
| 2026-08-17 | D-026 | Adopted | Committed destinations and selected abilities stay visible to the Party; face-down simultaneous commitment is rejected. Quarterbacking is designed out structurally, and decision concentration is the accountability. | Hidden commitment would break Committed Movement's collision rule and cost the shared-planning feel that makes the Timeline a party object rather than four private ones. The research itself concedes systemic ownership beats silence rules: when an answer needs more actions than one player controls, no one player can drive the fight. Rejected 2026-08-17: revision 2's Team-Up cards, which let a player act during another's turn. We have no separate turns — Quick and Slow are simultaneous windows with public commitments, so Team-Up solves a sequential-turn problem we do not have. The one genuinely missing piece, committing conditional on an ally's committed action, is D-028's attachment primitive aimed at an ally. | Decision concentration (D-027), via the policy-sweep proxy now and the human instrument once Parties play. If one player proposes nearly every response, the fix is more demand density — never a rule forbidding conversation. |
| 2026-08-17 | D-027 | Adopted | Four encounter metrics are required, each falsifying one adopted law: Timeline conversion rate (D-021), failure attribution (D-022), decision concentration (D-026), and role counterfactual value (D-025). **Amended 2026-08-18 (D-053):** the first is now *schedule learning rate* — whether a party acts on knowledge of the Boss's order they could only have got by playing it — because the Forecast Row it measured is gone. The law it falsifies is unchanged in kind: if the number stays flat, either the order is unlearnable or it is already memorised. They join the automated solo-victory and enrage-survival gates. Attempts-to-first-clear and wipe-cause distribution become required once repeat-clear content exists; the remaining research metrics stay opt-in. | Repeatability and fairness are a bundle, not a single win rate. Four metrics with a named law behind each will actually get collected; eleven will not. The two deferred metrics need multiple attempts by the same group, which the solo slice cannot produce. | Definitions land in the [deck-evaluation rubric](deck-evaluation-rubric.md); the decision-concentration proxy lands in the [measurement plan](../artifacts/deck-evaluation-measurement-plan.md). Each metric is collected at the first session that can produce it. |
| 2026-08-17 | D-028 | Adopted | The Timeline is addressable: a card may become a `Commitment` — an authored effect bound to one named Boss Beat whose parameters are disclosed (Incoming or Instant), visible to the Party, resolving when that Beat resolves. Target redirection and mechanic transformation are rejected for now. Fortify is reclassified as a Commitment against the next Round's Instant Row. | Revision 2 of the cooperative-boss research makes this the game's identity claim — the Boss writes the first draft, the party edits it. Attachment is the version that earns it without touching target selection: redirection would rewrite who a Targeted Boss Hit lands on, which is the Tank's core identity mechanism, and transformation needs a new authored vocabulary per Beat. Attaching to a Forecast entry is forbidden because a family-level entry states no parameters, so a Commitment there would be a bet rather than a plan. **Amended 2026-08-17.** The redirection and transformation bans are **effect-level**: they bind every mechanism, including an Enemy Status, because the reasons were about the effects and not the delivery. **Retracted 2026-08-17:** Fortify is *not* a Commitment. It prepares for whatever next Round opens with rather than for a named Beat, and next Round's Instant Row is visible only as a Forecast entry — which this decision's own binding rule forbids. Fortify stays a Status Effect with delayed onset (D-019), and Commitment therefore has no implementing content. | Engine seam: a Beat-bound effect the engine does not have. The payoff is that D-027's Timeline conversion rate becomes countable by construction — a prepared answer is an attachment, not an observer's judgement. First Party session for play-feel. |
| 2026-08-17 | D-029 | Adopted | `Encounter Responsibility` is canon vocabulary: a duty the encounter assigns, transferable during the fight, never tied to Archetype. Not required in every encounter. Its answers still obey D-025's asymmetric efficiency. | Destiny's Scanner/Operator/Suppressor show that encounter roles need not equal classes, and a transferable duty spreads decision concentration by construction (D-026). Requiring one per encounter would force duties into fights that do not need them — the coloured-keys failure D-025 rejected, in a new coat. The Archetype prohibition is what stops "only the Relic Bearer may…" from smuggling a role lock back in. | Multi-Hero Party model, then a Party session showing the duty changes hands and that holding it is a real decision. |
| 2026-08-17 | D-030 | Adopted | The Archive/Echo model is a bounded per-Boss option, not a general law: a Boss may send unresolved mechanics to an Archive and schedule at most one Echo per phase transition, and every Echo obeys the disclosure contract — a `Severe` Echo still reaches the Forecast Row. | Failure changing future content is more interesting than failure moving a counter. As a general law it makes every Boss a death spiral and gives us two escalation systems competing for one feeling; revision 2 itself scopes it to a single encounter template whose kernel is "everything you fail to solve comes back." The Echo disclosure rule prevents failure from becoming untelegraphed punishment. | A second Boss whose identity is memory. Embermaw does not take it — its kernel is fire and adds, not recurrence. |
| 2026-08-17 | D-031 | Adopted | Escalation Thresholds `1`–`4` may be structural rather than numeric, and Embermaw's two damage-bonus thresholds become permanent Scorch: `1` Ashen Verge burns the western edge, `4` Closing Jaws spreads around both western corners. The counted top threshold stays as the termination guarantee. No authored Scorch hex may be adjacent to the Boss. Amends [ADR 0027](../adr/0027-make-escalation-the-encounters-single-clock.md) in place. | Revision 2 names turn-counted death as the weak form of enrage and argues for collapse expressed through space — and it caught a real defect: Embermaw's thresholds `1` and `4` were both "attacks deal `+1`", the exact numeric inflation the Encounter Design Bible forbids, authored two commits after that rule was written. A fully structural top threshold was rejected because a structural condition can stall, and the automatic tick is what guarantees the fight ends. The adjacency ban exists because Scorching the Guarded Front would remove the Tank's own answer — the acceleration lesson in another form. | Sweep re-run: `victory%` `0%` everywhere and `checkpoint%` identical to the pre-Escalation baseline on all twelve prior policies. The enrage wall's standing criterion still holds, met by more than one policy at `0.00` Boss damage; the percentages recorded here originally have since been superseded by ADR 0027's correction. Scenarios regenerated. Not yet measurable: the anti-camping value of burning the western edge, because far-camping policies die in Round 5 to other pressure. |
| 2026-08-17 | D-032 | Adopted | Attaching splits into two concepts by lifetime anchor, with no umbrella term. A `Commitment` binds to a future **resolution** — an ally's Top Card or a Boss Beat — and resolves with it. A `Status Effect` binds to a **combatant**, widened from Heroes to any Hero or Enemy. Every attachment, of either kind, is visible to the whole Party. | The four requested targets divide on what the attachment is anchored to, and the two halves have different lifetimes, binding rules, and engine problems — instance identity versus payload vocabulary — so they ship independently. An umbrella term would invite the assumption that four targets share one rule set; the glossary already prefers repetition over abstraction, as `Tank Hit` and `Targeted Boss Hit` do. No separate `Enemy Status` term either: the mechanism, triggers, expiry, and catalog are identical and only the payload differs, and two names for one mechanism is the `Enrage Timer`/`Encounter Clock` duplication retired earlier the same day. Visibility follows D-026, the Healer no-blame-sink rule, and the fact that an attachment on an ally's card is useless if that ally cannot see it. | The Stat Panel extends to Minions and the Boss. Party-scale play-feel for the ally half. |
| 2026-08-17 | D-033 | Adopted | Status Effects become authored content: a catalog at `data/statuses/*.json` referenced by id, plus an `applies_status` card effect. Fortified's definition migrates into data; its Armor amount still rides the card. Riposte Ready stays in engine code. | Nothing in `data/` could apply a status — Riposte Ready and Fortified were constructed by engine code at hardcoded moments — so this was the real blocker, not the attach targets. A catalog rather than inline definitions because a status is shared vocabulary: three cards applying Sundered must mean one Sundered, with one title, one rules text, and one answer to whether it stacks. No ADR needed; ADR 0020 already decided gameplay content is schema-validated JSON in `data/`, so this is that decision applied. Riposte Ready waits because its graded consumption (D-015) is exactly what a first authoring vocabulary models badly. *(Revisited by D-064: the graded-consumption premise dissolved when the Signature Slot left one payoff route and no Counter, so the stay-in-engine clause retires with the migration increment; the catalog half of this decision stands.)* | Catalog validation rejects an unknown status id and a target/side mismatch; Fortified's D-019 arc still passes unchanged. |
| 2026-08-17 | D-034 | Adopted | An Enemy-facing status carries two named fields, `damageTakenBonus` and `damageDealtPenalty`, resolved inside existing damage resolution. Stacking is authored per status, defaulting to non-stacking. Targeting reuses the existing rule per kind: a Minion within `range_tiles`, the Boss with no range, an ally's Top Card with no adjacency. `target_type` becomes load-bearing. | `addStatus` always accepted any board entity, so a status on a Whelp already attached and did nothing — the payload was four Hero-flavoured numbers. Two named fields rather than a general effect list follows the ADR 0021 precedent and keeps Resolution Facts legible for the evaluation harness. Per-status stacking because canon already holds both behaviours: Riposte Ready never stacks, Fortified stacks additively. The Boss is range-free because requiring range would contradict the positionless `boss_damage` ruling, and ally support is adjacency-free because D-009 and the Healer Principles both chose against making support depend on huddling. | Engine tests cover a status on the Boss, raised damage taken, lowered damage dealt, a refused second copy, and a refused untargeted cast. No live-deck card applies a status yet: the first one changes the damage economy the D-016/D-017 walls were measured against, so it owes the deck-evaluation gate. |
| 2026-08-17 | D-035 | Adopted | Commitment binding mechanics, adopted but unbuilt: a Boss Beat gains an instance id minted when it enters the Timeline; an ally-Slot Commitment anchors to the Top Card **instance**, leaving when that card leaves the Slot; the vestigial `board_slot` target type is claimed as this feature's name. | Programs are re-read every Round, so the authored id `raking_claw` recurs and D-028's "named Beat" had no identity; minting at Timeline entry also gives Resolution Facts a stable handle, which is what makes D-027's Timeline conversion rate countable. A composite key is fragile because `refreshTelegraphs` re-aims a row twice per Round. Anchoring to the Slot position would let a buff land on a card nobody agreed to buff when the ally replaces its Top Card at Loadout, breaking the shared-plan contract D-026 defended; `CardInstance.instanceId` already provides the stable identity. | Deliberately unbuilt: minting makes the Timeline hold instances rather than authored data, and with Fortify retracted it has **zero** consumers. Build it when a card needs to bind to a Beat — that is also the ADR moment. The ally half additionally waits on the Party model. |
| 2026-08-17 | D-036 | Adopted | Every Boss Program must ask for a distinguishable answer, and a demand the deck can answer must be priced. Embermaw's five programs were the same six Beats under five names — `hunt` and `brood` identical except flavour text, and all five sharing one counter-tag union — so each Phase I program now leads a different demand: Hunt tests Mitigate and Move, Ember tests Move alone (Armor is wasted on it), Brood is the only program that spawns and the only one asking for `Kill Adds`. Brood Call is priced at `escalation_if_unanswered: 1` and rated `severe`. Phase II mirrors the split, ordered `ashfall, molting` so the unforecast Phase Break does not open on a severe Beat. | Identical programs make the Forecast Row a label rather than a horizon: a tag line that never changes discloses nothing, which is [ADR 0026](../adr/0026-disclose-boss-beats-in-stages-across-three-timeline-horizons.md)'s first accepted consequence coming due harder than it was written. Pricing Brood Call closes the matching defect on the clock: [ADR 0027](../adr/0027-make-escalation-the-encounters-single-clock.md) shipped acceleration authored at `0` pending a Whelp answer that was already in the starter deck — two copies of Sweeping Blow, `2` damage at range `1` against a `2`-Health Whelp — so the one lever that makes the clock respond to play was switched off through the entire pass that argued for it. | Three pairwise-distinct counter-tag sets, asserted over live content. `escFromAdds` nonzero across the sweep, having been `0.00` on all eighteen policies for 540 runs. `culler` (Sweeping Blow in Quick, walks to the Whelps) is the only policy reaching Round 8, so paying the adds tax is what buys the clock. D-003 restated as a live invariant — a priced Brood Call requires a deck card that can kill what it spawns — replacing a blanket-zero test that encoded one encounter's tuning and would have passed forever while the premise rotted. Observation, not criterion: the deepest solo push now leaves Embermaw at `4` Health of `48` rather than about `10`, with `victory%` still `0` across 1,200 runs. |
| 2026-08-17 | D-037 | Adopted | A Boss's authored program list is a pool, not a script: the running order is drawn from the encounter seed at setup, with Round 1 pinned to the authored opener and the remainder dealt from reshuffled bags of the whole pool. Promoted to [ADR 0028](../adr/0028-draw-the-boss-program-order-from-the-seed.md). | A fixed `(index + 1) % length` rotation made the Forecast Row decorative — next Round's program was deducible from the Round number, so the third horizon disclosed nothing a player could not already count. Setup is the only legal place to roll it: [ADR 0025](../adr/0025-randomize-before-the-window-that-answers-it.md) forbids resolving randomness after the window that answers it, and the row reads a full Round ahead. Bags rather than independent draws so *when* a demand lands changes without changing how much of it a fight contains. Round 1 stays authored because it is the teaching Round and the one Round the row can never have disclosed. | Measured before the change: with the player passing every window, the Round-by-Round program sequence was byte-identical across five unrelated seeds, and twelve of eighteen policies ended on exactly one Round across thirty seeds. After: `roundSpread` is a range for every policy, and that column now ships in the default sweep table so the failure cannot hide inside an average again. Structural gates in place of percentages — pinned opener on every seed, each program once per three-Round window, no back-to-back repeat while three are available, and `programSequence` never changing once a Round advances. |
| 2026-08-17 | D-038 | Adopted | Two measurement instruments, ahead of any further content work on variety. (1) `programPredictability` reports the best-possible accuracy of a perfect counter guessing next Round's program, estimated from real generated sequences with no knowledge of the generator's rule, and ships as a sweep footer with a red flag at full predictability. (2) A matched `forecast_reader` / `forecast_blind` policy pair, identical except that one consults the Forecast Row before spending its Slow Window on Fortify — the one card whose payoff lands next Round (D-019), and therefore the only spend the Forecast Row can price. | Both remaining flat things were unjudgeable rather than unfixed. "Countable" was an argument until it had a number, and [ADR 0026](../adr/0026-disclose-boss-beats-in-stages-across-three-timeline-horizons.md)'s whole third horizon was unfalsifiable because nothing in the sweep read it: a fixed script cannot benefit from information it never consults, so no amount of program differentiation could have been shown to help. Building either fix (more programs, or the Module Slot of D-024) before the instruments existed would have been tuning against an unmeasured quantity. | The predictability instrument found a real defect on its first run: the no-repeat rule swapped a colliding program into slot `1`, a fixed position, which leaked information and measured `68%` on Round 5 where a shuffle should give `50%`. Displacing to a uniformly chosen later slot fixed it — Rounds 5 and 8 fell to `53%`, mean `69.5%` to `66%`. Remaining: certain on R3 and R6, never below `50%`, against a `33%` uniform floor; the two certain Rounds are the cycle-end certainty a full-bag deal necessarily imposes, which is the ceiling on program-order variety and the standing argument for D-024. The reader/blind differential returned a **negative** result — see ADR 0026's amendment. Cost: the rng-stream change moved every seeded fixture; Scenarios regenerated, and the scripted first turn needed a new `random_seed` because its opening Hand is shuffle-derived rather than authored (recorded as a known fragility in its test). |
| 2026-08-17 | D-039 | Adopted | Mitigation protects standing room, not Health. A Beat may mark its Hazard `permanent`, so `hazard_last_impact` writes to the arena for good; and a hit absorbed on the Guarded Front for zero Health loss — the same predicate that grants Riposte Ready — spills its ash to a neighbouring hex further from the Boss instead of under the Tank. It never spills less. Embermaw authors this on Hunt Pattern's Ash Trail. | The sweep showed mitigation was inert: runs end at Escalation with Health to spare, so Armor bought nothing that reached the ending, and the matched `forecast_reader`/`forecast_blind` pair (D-038) could not move because there was no lever behind the Forecast Row's `Mitigate` tag. The [co-op boss research](research/2026-08-17-coop-boss-design-repeatability.md)'s Ashen Colossus template names the fix directly — *"positioning each hit determines the future geometry of the arena"*, with failure graduated so mistakes alter future game state rather than being binary checks. Displacement rather than prevention because Tank Principle 1 generalises: whatever currency mitigation protects, perfect play may change the shape of the loss, never the total. Preventing the burn would have been sustain creep in a new denomination. | Unit-tested in both directions: a hit that draws blood burns the Tank's hex permanently, a fully absorbed one burns a hex strictly further from the Boss, both burn the same number of hexes, and at the board rim the ash lands under the Tank anyway because the bleed must refuse to stop. **Measured dose is currently too low to constrain anything**: the Ash Trail accounts for `1.0` hexes of `19` for far-camping policies and `1.6` for dodging ones, against the `5` the Escalation Thresholds already burn, and `burnt` tracks fight length rather than play quality. Hunt is one of three programs and fights end at Round 6.4, so only about two Ash Trails resolve per run. The mechanism is correct and its weight is not yet real — recorded here rather than discovered later, which is the defect Brood Call's authored `0` already cost us once. |
| 2026-08-17 | D-040 | Adopted | `Drive Back` — Quick, push a piece `2` hexes directly away, range `1`, Charge `2` — added to the catalog and swapped into the starter deck for two Steady Strike (`6/6/2/2/2/2`). It gives Elian a repositioning verb that never leaves the Guarded Front, because it moves the Boss rather than the Hero. **Signed off 2026-08-18**: the swap is adopted, so `6/6/2/2/2/2` supersedes proposal 04's `8/6/2/2/2` as the live/default Shield Wall list. Proposal 04's list joins the `10x/10x` baseline as historical evidence. | Every flat result in D-038 traced to the Hero having one verb (Armor) against a Boss asking three questions. The catalog confirmed it: of the seven bench cards, six are "gain Armor" in different amounts and none moves anyone. Meanwhile ADR 0029's Forced Movement, ADR 0030's bursts, and card draw all shipped with no card using them — Brood Call's authored-`0` defect arriving fresh. Moving the Boss rather than the Hero is the shape the [co-op research](research/2026-08-17-coop-boss-design-repeatability.md) names for Tanks (*"the fight where the Tank has to move the boss between anchors"*) and it satisfies D-017: shoving costs the Guarded Front, so it is a trade rather than a free escape. | **The differential stayed flat, and now the reason is exact.** `forecast_reader` holds ~4 shoves per run against `forecast_blind`'s none, and at 200 seeds per position the two are identical at `far` (100/14/87, Round 6.37) and inside noise elsewhere. Drive Back trades Health (dodge the cone) for Health (next Round's unguarded bonus), and Health decides only `0`–`14%` of runs at `far`/`dodge` — Escalation ends the rest, and only the adds demand feeds Escalation. Positioning already answers every Health threat for free, which is D-017's unclosed range-camping P0 restated from the other side. D-016 holds at `victory%` `0` across 1,600 runs, but the ceiling thinned to `46` of `48`. |
| 2026-08-17 | D-041 | Adopted | Two new Beat kinds. `advance_toward_player` closes distance by an authored `move_tiles`, emitted as an ADR 0029 displacement with a third `advance` movement mode so the fact log names the Boss as the actor. `demand_proximity` prices standing out of reach: if no Hero is adjacent to the Boss at Round end, Escalation rises by the authored amount. Embermaw authors `Close the Gap` in Hunt Pattern at one tile, and `Within Reach` at `1` in Ember, Brood and Molting — never a phase opener, since a priced Beat is `severe`. ADR 0027's single-demand acceleration is generalised to a demand table rather than special-cased. | D-017's range-camping P0, and the root cause behind every flat result since D-038: standing at range 3 answered every Health threat for free, so no card or mitigation change could matter. **Boss movement alone cannot close it, and the measurement says so plainly** — advancing one tile per Round moved `turtle/far` from `0%` to `17%` Health deaths, and advancing *two* moved it back down to `9%`, because chasing a camper pushes them onto the Guarded Front where the claw loses its unguarded bonus. On this board being close is safer than being far, so the counter has to be a demand that being far *is* the failure of. | Camping is now dominated rather than optimal: `turtle/far` fell from Round `6.40` to `5.21`, and its Escalation-from-demands rose from `0.00` to `4.21` — roughly a tick per Round for refusing to close. `dodge` and `stay` are unchanged, so the pressure lands only on the position it was aimed at. D-016 holds at `victory%` `0` over 1,200 runs and far-camping Boss damage fell from `32.50` to `21.76`. Gates: enrage wall `554`, checkpoint discriminates 24/12, predictability `66%`. Two instrument defects fixed on the way: `escFromAdds` counted one demand reason by name and silently under-reported the second (`1.50` where the truth was `5.50`), and the smoke harness hovered a fixed Beat-chip index for a Beat with numbers. Review caught a disclosure bug before merge: the price was read from the whole program pool, so a Round running Hunt Pattern — which carries no proximity demand — was billed for a Beat the Timeline never showed, breaking ADR 0027's own disclosure rule from the inside. Demands now carry a scope: `pool` where the standing outlives the Beat that made it (a Minion is on the board whichever program is up), `program` where the Beat creates it for its own Round. A test drives a camping line and checks every charge against the program that actually ran; it fails against the pooled version. Fixing it also removed the zero-variance `5-5` spread that version produced, because which Rounds carry the demand now varies with the seeded order. One latent interaction is recorded rather than changed: Hazard entry damage is not filtered by entity kind, so an advancing Boss can walk into its own permanent Ash Trail and chip itself — which would credit a Hero with Boss damage they never dealt, against a D-016 margin already down to `2`. Measured at `0.00` across 60 runs, because the Boss advances one tile and occupancy stops it short of the burnt ground, so nothing is broken today. Whether an Enemy is immune to its own Hazards is a rules question, not a bug fix, and wants deciding before a Boss advances further than one hex. |
| 2026-08-18 | D-042 | Adopted | An Enemy takes no Hazard entry damage from a Hazard its own side laid. Stated per Hazard — every `apply_hazard` records the team of its source, and the Encounter itself counts as the Boss's side because a structural Escalation Threshold closing the arena (D-031) is the Boss's pressure rather than neutral weather. | Raised as a latent interaction in PR #77's review and ruled on after: Hazard entry damage was not filtered by side, so an advancing Boss could walk across its own permanent Ash Trail and chip itself, crediting a Hero with Boss damage they never dealt against a D-016 margin already down to `2`. It measured `0.00` because occupancy stopped the Boss short, but the hole widens the moment a Boss advances further than one hex. The rule is written as *own side* rather than *Enemies ignore Hazards* because a Hero-placed damage zone is exactly the card ADR 0030's bursts and ADR 0029's displacement invite, and it has to keep working against Enemies. | Three tests: the Boss crosses Enemy-laid ground unharmed, still burns on party-laid ground, and everything Embermaw puts on the board — Beat Hazards and Threshold Scorch alike — carries a recorded side. One existing forced-movement test was laying a Boss-sourced Hazard and shoving a Whelp into it; its subject is entry damage, not immunity, so it now lays the Hazard from the party. Gates unmoved: 226 tests, `victory%` `0` over 800 runs, enrage wall `554`, checkpoint 24/12, predictability `66%`. |
| 2026-08-18 | D-045 | Adopted | Round upkeep ticks every combatant's Status Effects, not the Party's alone, and the Stat Panel shows a piece's Status Effects whether that piece is a Hero, the Boss, or a Minion. A status whose host has left the board goes with it. | D-032 made the mechanism two-sided and D-034 gave the Enemy side a payload, but the Round's tick still ran inside the per-Hero loop, so an authored `duration_rounds` was inert on an Enemy: one Sunder marked the Boss for the rest of the fight, and Weakened lowered a Minion's bite forever. The clock is the half of a status that makes it a cost rather than a permanent rewrite of a piece, so an Enemy-facing status was not really shippable without it. Visibility follows the same argument: a rule the Party cannot read on the piece holding it is a rule they have to keep in their head, and the Stat Panel is already the one place a piece's live numbers are read. The upkeep runs after the Armor grant so Fortified's D-019 arc — pay out, then expire — is unchanged. | Engine tests cover an enemy status expiring at the Round boundary with the expiry recorded on the Round's own fact, a Minion taking the raised damage and its status leaving with it on defeat, and a status held by a departed piece being dropped. The Boss's panel was checked by hand in the built app with a seeded Sundered — chip on the Boss plate, authored rules text in its hold popup — and that check is manual only: no automated test drives the Stat Panel. Gates unmoved — no live-deck card applies a status yet (backlog item 10), so the damage economy the D-016/D-017 walls were measured against is untouched. |
| 2026-08-18 | D-046 | Adopted | Keywords become the one tag namespace, with a required `kind` (`role`, `trait`, `damage_type`, `answer`). A Beat's `damage_classification`, `target_selector`, and `counter_tags` become validated Keyword references rather than free strings; `raid_hit` is authored in `data/keywords/` rather than hardcoded in the Minion end step; `role_marker` is derived from `kind`; and the catalog asserts at load that every Keyword the rules name by id exists as the right kind. Keywords still carry no behaviour. | Four tag vocabularies had grown side by side and only `card.tags` was checked. `damage_classification` was the load-bearing one: the rules compare it against `tank_hit` to decide whether a Tank Hit grants Riposte Ready, so a typo in a Beat — or a renamed Keyword — disabled a Status Effect silently, at load, with every test still green. Its sibling value `raid_hit` was not in `data/` at all, which meant half the damage-type vocabulary lived in engine code where no designer could see it. `target_selector: "tank"` already named a real Keyword id and was joined to the catalog only by coincidence of spelling. One namespace because a tag is worth having only if something can pivot on it, and a `kind` discriminator because a reference that resolves to the wrong sort of Keyword is a category error the id check cannot catch. `role_marker` goes because being a Role is precisely why a Role Keyword is left off the glance surfaces — never two facts. This is the no-design-risk first phase of the keyword/counter sketch; the counter half of that sketch is unadopted and would reverse part of D-034, so it owes its own entry. | Four new validation tests, each failing without the check: an unauthored `damage_classification`, a reference resolving to the wrong kind, an unauthored counter tag and target selector, and a catalog missing a Keyword the rules name. A typo planted by hand in `embermaw_hunt.json` was confirmed to fail the build naming the Beat and field; the committed tests cover the same rules against a synthetic Beat rather than by mutating live content. Gameplay bit-identical: 255 tests, the browser smoke's replay fingerprint unchanged at `109bea72`, enrage wall `554`, checkpoint 24/12, predictability `66%`, `victory%` `0`. The Forecast Row still reads `Position · Mitigate · Kill Adds` — content stores ids, the projection resolves titles. |
| 2026-08-18 | D-047 | Adopted | Status Effects become Counters: a named marker with a count, a cap, and a clock, whose entire payload is a list of Readers. D-034's `damage_taken_bonus` and `damage_dealt_penalty` are removed, along with `applies_to` and `stacking`; a Counter no longer declares a side, and non-stacking is `max: 1`. Cards gain `places_counter`/`counter_amount` and a `reads` list with three verbs — `gate`, `scale`, `spend` — where a spend at `cost` timing is paid before the card's effects are computed. Readers may match by Counter id or by Keyword. There is no boolean composition and there will not be: gates AND, and a mechanic needing more becomes engine code. | D-034 chose two named fields over a general effect list to keep Resolution Facts legible, and that was right for two effects; it stops being right the moment the vocabulary has to carry a third, because each new effect is a new field on every Counter that ignores it. The Reader inversion puts the payload on the thing that reads the marker instead, which is what makes an archetype-scoped Counter possible at all: Ash means whatever the cards reading Ash say, so a Counter can ship before the cards that use it and a card can be written against a Keyword before the Counter exists. Fortified is the proof the model is not just rearrangement — its banked Armor became the count, so D-019's additive stacking is now addition rather than a flag, and `armorOnRoundStart` and the never-set `damageReduction` both left with it. Riposte Ready stays engine-built exactly as D-033 ruled: graded consumption (D-015) is what this vocabulary models worst. `on` is a closed set of two subjects, not a path expression, because that is the line between a reader vocabulary and a query language. | Behavioural identity is the headline: the full 36-policy × 30-seed evaluation sweep is byte-identical to the pre-change run across every metric, Riposte Ready grants and Fortify's Armor included — the sweep also caught the one real regression, an evaluator still reading the pre-rename fact keys, which no test would have found. 266 tests, lint and typecheck clean, browser smoke and headless replay green. New coverage: scaling off a target's Counters, a gate refusing at the legality predicate and at resolution alike, cost-before-scale versus scale-before-resolution timing, Keyword-matched reading, capped multi-placement, and six validation rejections including a Counter nothing can read. The record's state fingerprint moved, which is the state shape changing rather than the game. |
| 2026-08-18 | D-048 | Adopted | Counters gain two more hosts, `hex` and `slot`, and are keyed by a tagged host ref (`combatant:<id>`, `hex:<q,r>`, `slot:<hero>:<n>`) rather than by entity id. A Counter's `host` and its card's `target_type` must agree, and a Counter hosted anywhere but a combatant may declare no Readers. Slot Counters ride the prepared card, so re-loading the Slot drops them. **Escalation does not become a Counter, and `encounter` is not a host.** | Two hosts, one refusal. The hosts are the point: `hex` puts a mark on ground that outlives whoever stands there, and `slot` finally makes D-035's Top Card attachment reachable — `target_type: 'board_slot'` had been authored, rejected at load, and unbuilt since D-033. Keying by tagged ref rather than adding parallel maps keeps one upkeep loop, and the ref is what lets that loop ask "is the host still there?" without knowing which kind it was; the ref type is branded so the compiler, not a test, found every unmigrated call site. Non-combatant hosts take no Readers because every `when` in the vocabulary names a combatant's event — ground never takes damage — so a Reader there would be an effect that can never fire. The refusal matters more than the hosts: the sketch claimed Escalation was already this pattern, and it is not. Escalation counts, but its thresholds are one-shot band effects on the whole board — `scorch_hexes` mutates the arena, `extra_spawn_count` changes how many Whelps a Beat summons, `minion_damage_bonus` modifies every Minion — and none of those are `per`-count modifiers on a host, nor expressible in the four-value effect enum shared with Charge Modifiers. Converting it would mean widening `effect`, adding subjects like "every Minion" to `on`, and inventing on-crossing semantics: three concessions that would corrupt the Reader vocabulary to absorb one mechanic that was already fine. The counting is shared; the reading is not. | Gameplay identity again byte-identical: the 36-policy sweep matches the Phase 0 baseline on every field. 273 tests, lint and typecheck clean, smoke and headless replay green. New coverage: ground Counters placed, read, and surviving the Hero who stood there; a hex Counter refused off-board and out of range; a Slot Counter placed and dropped by the next Loadout's re-load; an empty Slot refused; ground Counters dying with the hex they sat on. That last test deletes the hex directly, and the honest reading is that no live mechanic reaches it: a structural Threshold lays a *permanent Hazard* on ground (D-031), it does not remove the hex, so Counters on burnt ground currently survive. Whether they should is an open design question, left open rather than settled by a liveness predicate. Two validation rejections besides — a card whose target cannot supply its Counter's host, and Readers on a non-combatant host. |
| 2026-08-18 | D-049 | Adopted | Damage carries Keywords, plural: a Beat's `damage_classification` becomes `damage_keywords`, and Cards gain the same field. A Counter Reader may name one `event_keyword`, answering only blows carrying it; a Reader that names none answers every blow of its kind. `event_keyword` is legal only on the two damage events, because a Round starting and a Slot firing are not made of anything. | Plural rather than singular because "who a blow is aimed at" and "what it is made of" are two axes, and one string holding both is exactly the category error the `kind` discriminator was added to prevent (D-044): a blow can be a Tank Hit *and* fire, and the singular field made that unauthorable. Cards gain the field because only Boss Beats classified their damage, so an event-Keyword Reader would have worked in one direction only — the party could be warded against a Whelp's bite but could never throw anything a Counter could answer. The filter is the whole of Phase 3: the fact stream already carried these Keywords and the rules already read one of them to grant Riposte Ready, so this types what was there rather than adding a mechanism. A missing keyword on a Reader is treated as "answers everything" rather than "matches nothing", so a hand-built instance degrades to the old behaviour instead of silently going inert. | Gameplay identity byte-identical to the Phase 0 baseline across the 36-policy sweep; the Riposte Ready gate now reads `damage_keywords.includes('tank_hit')` and grants at exactly the same rate. 279 tests, lint and typecheck clean, smoke and headless replay green. New coverage: a ward answering only its own Keyword and staying silent on others, a keyword-less Reader answering everything, one Keyword matching out of several a blow carries, a Hero's own card damage answered by a Counter, and two validation rejections — an `event_keyword` on a non-damage event, and a Card keywording damage it never deals. Also folded in a Phase 1 miss: the `status_bonus`/`status_id` pair inside a damage fact context, which the rename had left behind. |
| 2026-08-19 | D-050 | Adopted | Ground that burns away for good takes the Counters on it. A permanent Hazard — the arena closing under a structural Escalation Threshold (D-031) — clears that hex's Counters; a temporary Hazard does not. | The question D-048 logged rather than answered, and it had to be answered before a hex Counter could ship as anything but a curiosity. A structural Threshold has to cost the party, and a marked hex that survived its own burning would make the collapse a windfall for whoever had banked something there — the one place on the board where closing the arena *helped*. Temporary Hazards are the other way round: weather is something ground outlives, so the marks stay. Ruled at the moment the Hazard lands rather than inside the liveness predicate, so the fact log names the clearing instead of a Counter quietly vanishing at the next upkeep. | Two tests, both required by the mutation audit before it would pass: a permanent Hazard clears a marked hex and records `clearedHexCounters`, and a temporary one leaves it. Backlog item 13 closes. |
| 2026-08-19 | D-051 | Adopted | A Boss Beat can place a Counter. New Beat kind `place_counter` with `counter`, `counter_amount`, and `counter_target` (`self` or `hero`). Embermaw uses it for **Heat**: `Stoke the Forge` banks one Heat on the Boss each time the Ember program runs, and every Heat it carries adds 1 to every blow it deals. Heat has no clock — it never cools on its own. | Counters shipped as a Party verb only: the party could mark the Boss and the Boss could not mark back, which reads as an oversight the moment anyone plays it. `counter_target` carries both directions because the interesting one is `self` — pressure the party watches accrue and can choose to spend a card cooling — while `hero` is the plain debuff that closes the asymmetry. Homed on Ember, a Phase I program, after measurement proved the first home was decoration: on Molting (Phase II) the Beat resolved **zero times in forty runs**, because Phase II opens at Round 5 and the slice ends there. A Beat that never fires is not shipped, it is decorated, which is the same failure D-049's readable-pairs rule exists to stop. | Heat costs the Party real Health without touching the teaching Rounds: HP deaths rise `30.60% → 34.60%` and clock deaths fall `69.40% → 65.40%` across the 30 shared policies, while the Round-4 checkpoint holds (`90.90% → 90.47%`, still 24/12 discrimination) and `victory%` stays `0`. Four tests plus two mutants: the mark, its direction, the cap refusing past `max`, and three load-time rejections. |
| 2026-08-19 | D-052 | Adopted | Quench is the first Card that reads a Counter: 2 Boss damage plus 1 per Heat, then 2 Heat removed at `resolution` timing. It ships in a new **evaluation deck** (`data/decks/aegis_heat_answer.json`), **not** the live starter deck. The sweep gains `--deck <id>` so a candidate deck can be measured without being promoted. | Backlog item 10 asked for a live-deck card and the automatic evidence does not support one. Trading two Steady Strike for two Quench costs the plans that do not build around it 22% of their Boss damage (`8.79 → 6.81`), and the rubric is explicit that default-deck promotion needs the human half — class fantasy, meaningful choice — which no sweep can produce. So the card is authored, measurable, and out of the live economy until Design scores it. The `--deck` flag is what makes that position tenable: before it, a candidate card could only be measured by promoting it first, which is the promotion the measurement is supposed to gate. Its first version was worse than that — 1 base damage, all its value in a Phase II payoff — and measured at `2.71` Boss damage, half of anything else; a 2 floor took it to `5.29`. | On the candidate deck a plan built around Quench clears the Round-4 checkpoint on 100% of runs in four of six variants and reaches `7.80` Boss damage at its best, against a baseline policy mean of `8.79` with two more Steady Strike. `victory%` stays `0` on every deck measured, so D-016's red flag is untripped. Three tests: the scale-then-spend arc, the cold floor, and that the live deck does not carry it. |

## Next Decision

After the controlled deck playtest, decide whether the five-identity test deck creates sharper Slot Tension than the two-card baseline without obscuring first-time learning. This is a creative tradeoff requiring player evidence, not an engineering choice.
| 2026-08-18 | D-043 | Adopted | A Boss Beat's reach is authored content. `bossBeatSchema` gains `range_tiles`; `forward_cone` and `demand_proximity` read it, and the catalog rejects a ranged Beat that omits it *and* any other kind that declares one. Embermaw authors `2` on all three Cinder Breaths and `1` on all three Within Reach Beats — the values already in force, so no behaviour changes. `targeted_hit` stays rangeless by rule, not by omission. | Reach was three engine constants for two Beats: a default parameter on `forwardCone`, a *second* literal `2` at the telegraph call site, and a bare `1` in the Escalation step. The two cone constants agreed by coincidence, so ADR 0026's "the telegraph must not lie" rested on nobody editing one without the other. The only place a reader could find any of these numbers was inside a `rules_text` string, which nothing validates. ADR 0020 puts this kind of decision in `data/`, and reach in particular is the whole difference between a Beat footwork answers and one it does not. Raking Claw's rangelessness is the same decision in the other direction — D-017 wants a hit that cannot be walked out of — so it is now enforced rather than assumed. | Five tests. The telegraph and the resolution are compared against each other rather than against a literal; shortening the authored reach must shrink both together, which fails against the old two-constant form; widening the proximity demand must stop billing a Hero two hexes out; and both halves of the validation rule are exercised. Behaviour-neutral by measurement, not by argument: 256 tests pass unmodified — including the recorded Scenario replays, which are action-by-action and would break on any drift — and the sweep is unmoved at `victory%` `0`, D-016 ceiling `46` of `48`, checkpoint 24/12, predictability `66%`. |
| 2026-08-18 | D-044 | Amended by D-056 | **The audit is still a pre-merge gate; since D-056 it runs in `npm run verify:local` and the pre-push hook rather than in CI.** The mutation audit (`npm run mutate`) is a pre-merge gate. It reintroduces each documented rule's defect one at a time and fails if the suite does not notice. A survivor is fixed by writing the missing test, never by weakening the entry. | A green suite says nothing about which rules it guards. Run against nineteen documented rules, the first audit found four unasserted — cumulative Escalation thresholds, the proximity demand's distance, the Hazard side fallback, and the Guarded Front half of the absorption predicate. Each was documented, implemented, and editable to something else entirely with every test still passing. The worst was D-042's: a test that walked a whole fight asserting the right property, but opened by assigning `escalation = 4`, which skips the threshold *crossing* that lays the only Hazards reaching the branch it existed for. All four share one shape — the rule lives in a bare literal or operator that no assertion names — and that shape is invisible to coverage, which recorded those lines as covered. | 19/19 caught after the four guards were written, and each guard was verified by reverting to its defect and watching it fail. The audit names the test that caught each rule, so a rule silently guarded by an unrelated test is visible rather than assumed. A stale anchor is reported as failure, not skipped, because an audit that quietly stops auditing is the failure it exists to catch. Cost is one suite run per entry, roughly 50s in CI. |
| 2026-08-18 | D-053 | Adopted | The Forecast Row is removed (ADR 0031). `forecast()`, the third row on the Boss Timeline, its hold-popup, and the matched `forecast_reader`/`forecast_blind` sweep policies are all deleted; `forecast.ts` becomes `consequence.ts` and keeps only `highestTier` and `programCounterTags`. `Incoming` and `Instant` are untouched and now disclose completely, so a Beat is never legally incomplete. The schedule is learned by playing, the way a Guild Raid boss is in *Tacticus*, and the catalogue of a Boss's moves belongs in a boss guide reached from the menus rather than on the play surface. `consequence_tier` is kept: it no longer sets a Beat's earliest legal horizon, it enforces that the first program of every phase carries no `Severe` Beat (D-036). | **The row failed the acceptance gate ADR 0026 wrote for it** — "whether the row is actionable information or decorative UI". Across 200 seeds per position the reader and its blind twin finished in the same Round to two decimal places at `far` (`5.14`/`5.14`) and `stay` (`4.79`/`4.79`), differed by `0.09` at `dodge`, and cleared the Round-4 checkpoint at identical rates (100/100, 100/100, 50/50). ADR 0026 had already accepted that the row "carries little information for an experienced player" under a deterministic rotation and shipped it anyway; the rotation was then replaced with a seeded order (ADR 0028) as the intended fix, and the row still measures zero. The structural reason is the law that governs every flat result in this slice: a decision only matters if it is priced in the currency that ends the fight. Escalation is that currency — `2,756` runs in the same sweep died to the Clock at `0` Boss damage — and foreknowledge cashes out only in Health and Boss damage. No honest pricing of foreknowledge in Escalation exists, because knowing about a demand a Round early does not answer it. | Behaviour-neutral and measured as such: all 24 policies that exist on both sides of the change are identical to two decimal places in Round and Boss damage, and the new `shover` policy reproduces the old `forecast_blind` exactly. `victory%` `0`, D-016 ceiling `46` of `48`, predictability `66%` against a `33%` floor, mutation audit `19/19`. 274 tests (six Forecast Row tests deleted, one kept in transformed form: the property that Round 2 is not deducible from the Round number was the row's justification and is now the design's foundation). `programPredictability` outlives the row it was built to defend and changes meaning — with nothing disclosing the schedule, that number *is* the learning curve, wanted in the middle of its `1`-to-uniform band rather than at either end. Two costs accepted openly: a first-time player has strictly less information during their first fight, mitigated by the pinned authored opener and the no-`Severe`-opener rule; and no fixed policy can learn, so the sweep now measures the floor of any decision whose payoff depends on knowing next Round — Fortify's banked Armor most of all — never its ceiling. That one wants a human playtest, not another policy. |
| 2026-08-18 | D-054 | Adopted | The `warning` Beat kind is removed. Its five instances across the five Boss Programs are deleted rather than migrated, and one clause that existed nowhere else — *"Leave yourself somewhere to stand."* — is appended to the `scorched` Hazard's own rules text. A Beat that resolves to nothing is not a Beat; rules reminders belong on the Hazard, Beat, or Program they describe. | Preparation for representing every Boss Beat as a card, in the game and in a future board-game port. A card that appears, gates a press, and changes nothing is the same decorative surface the Forecast Row turned out to be — and at five of twenty-five Beats it was 20% of the deck. Checked before deleting rather than assumed: every warning restated text already carried elsewhere. `ash_warning` duplicated the `scorched` Hazard (*"blocks voluntary movement while present"*), `heat_warning` duplicated `brood_call` (*"Any Whelp still standing at the next Round end raises Escalation by 1"*), and `lane_warning` duplicated Hunt Pattern's own program text (*"Hold its front, then leave the marked breath cone"*). Migrating them to a `reminder_text` field, as first proposed, would have created a second copy of a rule with nothing keeping the two in sync — the exact drift this project keeps finding. | Behaviour-neutral by measurement: `warning` resolved to nothing, and the sweep confirms it — zero drift across all 30 policies in Round, Boss damage and Escalation, the smoke suite's record fingerprint byte-identical at `109bea72…`, 274 tests, mutation audit `20/20`, `victory%` `0`, D-016 ceiling `46` of `48`, predictability `66%`. No counter tag is lost either: every tag a warning carried was already contributed by a resolving Beat in the same program, so `programCounterTags` is unchanged for all five and D-036's program-identity assertions never move. The handoff's Beat-kind list was corrected at the same time — it had been stale since D-041, missing `advance_toward_player` and `demand_proximity`. |
| 2026-08-18 | D-055 | Adopted | Every Boss Beat is a card, and the card is the control that resolves it. The playout's Continue bar is replaced by a Beat card that deals in above the board and prints everything the Beat resolves against — track, title, damage and unguarded bonus, target, reach, advance, spawns, what it leaves and for how long, and what it costs unanswered — plus its rules text and counter tags. `demand_proximity` is the exception and docks instead: it resolves to nothing when it plays and is priced at the Round end, so its card stands from the Incoming Row through the Slow Window and reports live whether the demand is currently answered. | Preparation for a board-game port, where a Beat has to be printable. The pacing was already right — the playout has held every Beat behind a press and named it first since the paced-playout work — but the prompt was a bar carrying a title, and everything that makes a Beat legible sat one hover away on a chip most players never hold. Two consequences beyond presentation. First, `beatCardStats` is the port's authoring gate: a Beat whose resolution consults something the card does not print cannot go on a table, and a test asserts that property over live content rather than one example, so a new authored field that changes an outcome and never reaches the card fails at once. Second, the docked demand is what makes a standing condition playable rather than announced — it is the card that stays face-up in front of the Boss on a table. D-043 is load-bearing for both: reach became content the day before, so the card and the Round-end check read one number. | 284 tests, 24 mutation entries, `20/20` before the card and `24/24` after. The audit was re-rooted at `src` so it can guard UI rules, and it earned that immediately: of four new card mutations it caught three and found a survivor in code written minutes earlier with tests — the phase guard on the docked demand, whose test used Round 1, a Round with no demand to withhold, so removing the guard entirely left it green. The same shape as D-042's defect, found the same way. Smoke passes with the record fingerprint byte-identical at `109bea72…`; no engine behaviour changes. One brittle smoke assertion replaced on the way: it required the prompt to contain the literal word "Continue", which made a label load-bearing — the verb is now "Resolve", chosen because "Play" is what a Hero does with a card in hand and a Boss card must not borrow a player's verb. |
| 2026-08-19 | D-056 | Adopted | GitHub Actions deploys the Workbench and does nothing else. `verify-workbench.yml` is deleted, along with `web/scripts/setup-local-runner.sh`, which existed only to stand up the self-hosted runner that workflow wanted. Every validation the repository has — the browser check, the Vitest suite, lint, the production build, the browser smoke suite and the mutation audit — is `npm run verify:local`, one command, cheapest first, and it runs on push through the pre-push hook. | Each layer of CI left for the same reason, and the pattern is the argument. The browser suite went first: `playwright install --with-deps` shells out to apt-get on a hosted runner, and on 2026-08-18 a slow Ubuntu mirror wedged three runs for 14 to 24 minutes each and then failed them — a gate that fails for reasons the author cannot act on is one everybody learns to ignore, which is worse than not having it. The replacement was a self-hosted runner, which is a machine that can be switched off: the gate's honesty then depended on somebody's laptop being awake, and because a skipped job reports success, "green" quietly stopped meaning the browser contract had been checked at all. The remaining hosted jobs were fine but split the gate in two, so the answer to "is this change good?" lived half in the repository and half in a service. It now lives in one place that anyone with a checkout can run and fix. | `npm run verify:local` passes end to end on the merged tree: 352 tests, lint, build, smoke, mutation audit `32/32`, exit `0`. Two things are given up openly. A pull request from a fork is no longer validated by anything automatic — the hook runs on the contributor's machine or not at all — which is acceptable while this is a single-author repository and is the first thing to revisit if that changes. And nothing now blocks a merge mechanically; the gate is a thing a human runs, which is exactly the trade being made, since the previous mechanism was a check that could report success without having run. |
| 2026-08-19 | D-057 | Adopted | The Slot state `Primed` is renamed `Full`. The glossary term, the `Loaded` entry's `Avoid` line, the rules docs, the interface direction, and the Workbench's slot-state name, subtitle label and lock-head state all move to `Full`; `Primed` becomes the word to avoid. The frozen GDScript client (ADR 0019) keeps `primed` internally, where it already drew the flag as `FULL`. | The state is the one a player reads off a full Charge Stack, and `Full` is what they already say out loud — the Godot client printed `FULL` on the plate while the vocabulary said `Primed`, which is the vocabulary losing to the interface. The distinction the old name protected is not between `Primed` and "full": it is between a full stack that can still fire and a full stack that already fired, and that pair is carried by `Full` against `Fired`/spent, which have their own label, lock head and accent. | No behaviour changes: the state is derived in the UI and the engine never named it. The suite, lint and build stay green, and the `Full` subtitle and lock head render on a full unactivated Slot. |
| 2026-08-19 | D-058 | Adopted | A Counter left at its cap is a standing demand, priced in Escalation like an unanswered Minion. `place_counter` joins `spawn_minions` and `demand_proximity` in the Round-end step; the threshold is the Counter's authored `max`, read from the Counter rather than authored on the Beat, and the demand asks wherever the Beat actually places it, so a Counter piled on a Hero is priced the same as one banked on the Boss. Stoke the Forge banks the cap in one Beat and costs `1` unanswered; Heat caps at `2`, which is exactly what one Quench draws off; and Quench is promoted into the starter deck, trading two Steady Strike for two Quench. | The Counter system paid out only in Boss damage, and the sweep said what that was worth: trading two Steady Strike for two Quench cost the damage plan 21–27% of its damage and moved the Round the run ended on by 0.00 — 5.00 to 5.00, 6.50 to 6.50, 4.28 to 4.28, on 200 seeds a position. A decision that cannot reach the clock is not a decision, so Heat is now priced in the currency that ends the fight. Two things fell out of the draft that were not visible before it. The first is that pricing at the cap needs the cap to be *reachable*: Embers comes up about once in a five-Round run, so a `max: 4` Counter placed one at a time peaks at 1 and the demand was authored, correct, and dead — a new sweep instrument, `peakCtr`, is what said so, because every aggregate downstream of a demand that never fires reads exactly like a demand that does nothing. The second is that D-003 — no pricing a demand the deck cannot answer — was written as a test about `spawn_minions` and nothing else, so it did not travel to the next demand kind: Heat was priced against a deck with no way to cool it and the whole suite stayed green. That guard is now stated per priced Counter, and it is what makes the Quench promotion mandatory rather than a matter of taste. A third, found reviewing the diff rather than by running it: the Round-end step prices one demand per kind, taking the dearest priced Beat in the pool and asking *its* question, so two priced Beats of a kind that disagree would leave the cheaper one authored with a price and silently never billed. Content that does that is now refused at load, naming both Beats and the field they disagree on. | 356 tests, mutation audit `35/35` with the three new entries caught, `npm run verify:local` green end to end. The sweep on the shipped deck, 200 seeds a policy: `quencher/dodge` runs 6.00 Rounds against `dual_steady/dodge`'s 5.00, and is the only plan in the table whose `roundSpread` is not pinned — `5-7` against `5-5` everywhere else. Reported openly against that: the price costs roughly 1.5 Rounds of fight length across every plan that does not play the answer, and pins their spread flat, which is the staleness signature this whole line of work has been chasing. The sweep's policies are fixed slot plans, so those rows are a party that never plays a card now sitting in its deck rather than a reading of the game; the honest next measurement is a policy that plays Quench reactively instead of every Round. The committed solo-ceiling scenario ends at zero Health again rather than at the clock, a direct consequence of two Steady Strike leaving the list. |
| 2026-08-19 | D-059 | Adopted | The sweep gets `reactive_quench`: `dual_steady`'s slot plan until the Counter Quench answers is standing at its cap, and `quencher`'s for exactly as long as it is. Which Counter and which stack are read off the content — Quench's own `spend` reader and that Counter's `max` — never named in the harness. Two columns arrive with it: `ctrSpent`, the Counters the party actually drew off through a Card's `spend` reader, and (from D-057) `peakCtr`, the highest a *priced* Counter ever got. `peakCtr` is narrowed here from its first form, which measured every Counter but Riposte: reviewing the diff, a `turtle` run was reporting `5.20` off its own Fortified stack while the Counter the demand actually prices sat at `2` — a column answering a different question than its own comment claimed, which is precisely the failure it was added to catch. | The fixed plans bracket a decision rather than making one. `dual_steady` never plays the answer now sitting in its deck and `quencher` gives up a Steady Strike slot every Round whether or not there is anything to cool, so D-057's headline — Quench buys a Round — was measured against a party that plays it wrong in both directions, and the flat `5-5` rows it reported were a script's refusal to react, not a reading of the game. The swap lands a Round late by construction and that is the decision, not a defect: Stoke the Forge banks the cap in the Instant Row after Loadout has closed, and a Slot already holding a card cannot be swapped outside Loadout, so a party that did not pre-load on a hunch pays one Round-end bill and clears it on the next Loadout. `ctrSpent` exists because a reactive policy can silently decline to react, and a swap that never fires reads exactly like `dual_steady` in every other column — the same way a demand that never fires reads exactly like a demand that does nothing, which is the trap D-057 fell into and needed a new instrument to climb out of. | 200 seeds a policy, shipped deck. On `dodge`, `reactive_quench` runs 5.63 Rounds against `dual_steady`'s 5.00 and `quencher`'s 6.00, while keeping 17.69 Boss damage against 17.84 and 8.46 — 99% of the damage plan's output for two thirds of the clock the always-on plan buys. On `stay` it beats never-answering on both axes at once: 4.47 Rounds and 16.04 damage against 4.00 and 15.35. On `far` it buys nothing (5.00 against 5.00), which is the correct reading rather than a null one — a camper dies to the proximity demand, and cooling Heat does not answer that. `roundSpread` returns to `5-7` on the dodge line where every non-answering plan is pinned `5-5`. `ctrSpent` reads 1.46 against `quencher`'s 1.77 and `dual_steady`'s 0.00, which is the evidence the swap actually fires. `npm run verify:local` green: 356 tests, mutation audit `35/35`. |
| 2026-08-19 | D-060 | Adopted | The Boss Program strip is removed from the play surface. The `Instant` and `Incoming` rows, their beat chips, the strip's collapsible `HUNT PATTERN` header, and the two Detail Popups they held (`programDetail`, `beatDetail`) are all deleted; `ProgramStrip.tsx` goes with them. The Escalation gauge the header carried moves into the phase band, on its own line under the Round track and the full width of it, so the two readouts stack: where the Round is, then how far the fight has run. Nothing in the engine changes — the rows are still how a Boss Program resolves (ADR 0024), they are just no longer a band of chrome. | The strip was the last surface still listing Beats by name away from the moment they matter, and D-055 had already moved that job: every Boss Beat is now dealt as a Beat Card carrying its damage, target, reach, rules text and answers, and the Row pauses on each one. Two surfaces named the same Beats, and only one of them was where the player was looking. The strip cost 102 of 844 points on the 390×844 contract — the largest chrome band above the board — against ADR 0031's own standard, that the play surface is not where a Boss's catalogue lives and a row that measures nothing should be the first to give up its space. | Behaviour-neutral by construction: no engine, content, or store file changed, 362 tests pass and the browser session's record replays headlessly to an identical final state (fingerprint `94a64d53…`). Smoke suite passes, with three contracts rehomed rather than dropped — the paced Row's beat count now comes from the fact log's `Boss Beat:` lines instead of the strip's chips (the whole Row resolves when its window opens, so they are all there before the first press), the promise-versus-played check now compares Beat ids through `data-playing-beat` on the advance rail, and "some Beat carries its numbers" is walked across the Row's Beat Cards. Layout re-measured at 390×844: one band above the board where there were two, 101 points against 102 + 61, so the board container gains 61. The gain is headroom, not larger hexes — the canvas is width-bound at that viewport — which leaves the centring defect the interface direction already names as the next thing worth reclaiming. One accepted cost: an `Incoming` Beat's *name* is no longer readable before the Quick Window that answers it. The threat itself still is, on the board, as the telegraph it has always been — the breath cone and the marked spawn hexes are painted before the Row resolves, and the design bible's clarity pillar rests on those, not on the chips. What a player loses is the word "Brood Call" a window early; what they keep is the hexes it will land on. Wants a human playtest rather than another policy, the same instrument D-053 asked for. |
| 2026-08-19 | D-061 | Adopted | Decision-log ids are assigned at authoring time, not picked. A new row carries `D-NEW` in the ID column; `npm run log:ids` reads the log as `origin/main` has it and as the merge base had it, assigns a number to every placeholder, moves any number this branch invented that upstream has since invented too, moves the later of two rows holding one number, and carries the new ids into any file on the branch that cites them. It runs inside `verify:local`, so the pre-push hook refuses a push that would land an unnumbered or colliding row. The arithmetic is a pure module under `src/content` so the vitest suite and the mutation audit can both reach it; the git plumbing is the script. | The ids are one global counter and this repo keeps more than one branch open, which is a collision generator rather than an accident: D-045/046/047, then D-050/051/052 taken by another branch *while the first collision was being renumbered*, then D-057 taken by the Primed-to-Full rename while the Counter-pricing branch was open. Three times, and the third one landed after the check written for the first. That check — `decisionLog.test.ts` — was doing its job and still could not prevent this, because it reads the merged file: by the time it fires, both rows exist, and the fix is a renumber plus every citation of the wrong number. What was missing is that the author picked the number from a snapshot. The merge base is the piece that makes assignment safe rather than destructive: without it, every id this branch shares with upstream looks like a collision, and renumbering a row that merged weeks ago would break the citations the log exists to serve. The `D-NEW` placeholder is deliberately not a number — `D-000` or `D-999` is a number somebody eventually authors for real. | 38 mutation entries, `38/38` caught, three of them new and aimed at this module: ignoring the merge base, assigning over a number a later row in the same file holds, and rewriting ids globally instead of in the ID column. 15 tests over the planner and the merged log, including the placeholder never reaching a merged file. Reproduced end to end against the real history: branched from `6aaa610`, added a row taking D-057 and a `D-NEW` row, and the check named both (`D-057 -> D-061  (already taken on origin/main)`), `--fix` renumbered them and carried the new id into a branch file citing it, and a re-run went quiet. `decisionLog.test.ts` stays as the last line of defence for two branches pushing inside the same fetch window, and now shares the one row parser rather than keeping a second regex of its own. |
| 2026-08-19 | D-062 | Adopted | Raking Claw is a melee Beat. `targeted_hit` joins `forward_cone` and `demand_proximity` in `RANGED_BEAT_KINDS`, so it must author `range_tiles` and the catalog refuses a claw without one; the resolver gates its damage on `hexDistance(boss, Tank) <= range_tiles`, and a claw that reaches nothing writes no impact, so `hazard_last_impact` keeps the hex the Boss last actually connected on. Embermaw authors `1` on all three claws. The `+3`/`+5` `unguarded_bonus` is deleted with the rangelessness that made it reachable, and the counter tags become `Mitigate, Move`. The field itself stays in the schema and the engine for a Boss whose claw reaches further. | The selector was answering two questions at once. `Target: Tank` says *who* a Tank Hit is aimed at, which is what stops it being resolved against whoever stands in a pattern; D-017 also made it say *whether*, because in 2026-08-17 nothing else could hold a camper to anything. D-041 built that something else — `demand_proximity` prices standing out of reach in Escalation, the currency that ends the fight — and the claw was left as the one Beat whose reach was infinite for a reason that had moved somewhere better. What remained was a Beat whose fiction and rule disagreed: a claw that reached across the arena, and a player reasonably asking how. The `unguarded_bonus` had to go with it, and the geometry is the argument rather than taste — `turn_toward_player` snaps the facing onto the Tank before the claw resolves, and a snapped facing puts **every** hex within reach `1` *on* the Guarded Front. Measured, not asserted: all six adjacent hexes report `guardedFront true` after the turn. So a bonus for standing in reach unbraced would price a position the board cannot produce, which is the authored-but-dead shape D-058 needed a new instrument to catch. One cost is accepted openly. The `guardedFront` half of D-039's absorption predicate is now unreachable in Embermaw's live content for the same reason, since the claw is the only Beat carrying the `Tank Hit` Keyword; its test builds the case by resolving the claw without the turn, and says so, rather than being deleted along with the content that used to produce it. | The sweep moved 6 of 42 rows and nothing else, 200 seeds a policy. Camping gained almost nothing, which is D-041 holding: every `far` row on `dual_steady`, `turtle`, `culler` and `shover` is identical to two decimal places, because those runs were already dying to the Clock at `5.00` with `0%` Health deaths and the claw was not what was killing them. The rows that moved are the two that had been losing campers to Health — `quencher/far` `11%` to `0%` and `reactive_quench/far` `2%` to `0%`, both converting into Clock deaths at `100%` enrage and about a tenth of a Round longer — and `shover/stay`, which gains the most in the table: `hpDeath` `60%` to `48%` and checkpoint `41%` to `53%`, because shoving the Boss out of reach now answers a Beat it previously did nothing about. Every `dodge` and `stay` row that is not the shover is unmoved, which is the correct reading: they stand inside reach `1` and take the claw exactly as before. Gates hold — `victory%` `0` on all 42 policies with no red flags, enrage wall `3741` to `3787`, Round-4 checkpoint discriminating 28/14, predictability `66%`, peak Boss damage `36` of `48`. `npm run verify:local` green end to end: 370 tests, lint, build, smoke with the headless record replay matching, mutation audit `38/38` with three new entries (the reach gate, the impact memory, and the board refusing to lunge for a claw that reached nothing) and one rewritten, since the entry guarding `targeted_hit`'s rangelessness was guarding a rule that no longer exists. The frozen GDScript client and its `.tres` resources are left as they are, per D-018. |
| 2026-08-19 | D-063 | Adopted | A Minion may carry a fuse. `explode_damage` and `explode_radius` are authored on the Minion, both or neither, and a Minion carrying them detonates on the `Incoming Row` of the Round after it arrived — before that Row's Beats — dealing its blast as a Raid Hit to every Hero within the radius and to nothing else, then leaving the board. Embermaw's Whelp takes `3` at radius `1`. A detonation is not a `Minion Defeat`: no damage action removes the piece, so nothing records `target_removed`. Brood Call keeps its `escalation_if_unanswered`, but it is charged where the fuse runs out rather than at a Round end the Whelp can no longer reach — once for the Round, whatever the brood's size. The board paints the footprint for the whole Round the fuse burns through, as the third warm telegraph: it takes the cone's step, because a blast landing in the `Incoming Row` is exactly as imminent as the breath burning in it, and separates by shape rather than colour — a quiet wash with one outline around the outside of the union of every live footprint. | User-requested mechanic. It converts "Kill Adds" from a creep with an open-ended deadline into a hard one-Round timer, which is the same demand stated so a first-time player can read it off the board. Two answers rather than one is the point: kill it, or step out of the blast — but only killing it answers the *demand*, so footwork buys Health and never the clock. Heroes-only follows `Burst` pointed the other way; without it a party could bait a Whelp against Embermaw and be credited Boss damage it never dealt, which is the hole D-042 closed for Hazards arriving through a different door. Charged once per Round rather than once per Whelp because the Round-end demand it replaces charged at that rate: per-Whelp bills the same total in one Round and, measured, pulled the collapse forward roughly half a Round across the sweep and halved the policies clearing the Round-4 checkpoint. | Engine tests cover the held fuse, the detonation and its ordering ahead of the Boss, Heroes-only, the missed blast, the threshold bonus reaching the blast, the Escalation charge and its absence when the brood is cleared, and a fuseless Minion. Five mutation-audit entries guard the fuse's timing, its radius, its Heroes-only footprint, the piece being consumed, and the Escalation charge; the audit runs 40/40. Sweep re-run after merging main, which had landed D-062's melee reach into the same encounter: no red flags, `victory%` `0%` on all 42 policies, the ADR 0027 enrage wall holds at 510 qualifying runs, and the Round-4 checkpoint discriminates 28/14 — identical to main measured alone, so the fuse moves the checkpoint not at all and the wall from 596 to 510, which is runs ending in Health death rather than reaching the clock. (Against the older base this branch was cut from, the same three gates read 0%, 500 and 24/18, also identical to that base.) The measured shape change is the intended one: `stay` policies now die to Health rather than to Escalation (`sword_shield/stay` `hpDeath%` `47` → `100`), and `dodge` policies live about half a Round longer because a Whelp they stepped away from removes itself. Two pool-scope Escalation rules — D-041's demand scope and ADR 0027's spawn-Round grace — no longer have live content that exercises them and are now proven against a fuseless, priced catalog variant. The telegraph's own claims are held in tests: `boundaryEdges` is checked against `axialToPixel` rather than against a transcribed table, the outline is asserted to close around one union and to follow a footprint clipped at the rim, and the palette holds the split the blast's size forced — the warning in the edge at full chroma, the wash under it below both the Boss sprite and the spawn telegraph. Verified in the browser at the Round 3 jump point: the blast wash, the spawn telegraph and Scorched ground read as three different things on one board. |
| 2026-08-19 | D-064 | Adopted | Every Hero gains a **Signature Slot**: a third, always-present Action Bar Slot whose Top Card is printed on the Hero — never in the deck, never drawn, never replaceable at Loadout, and never discarded. It carries two clauses. The **standing clause** is a Grant, the mirror of a Counter Reader: it answers one event from the same closed `when` set, narrowed by `event_keyword` (D-049), behind a closed all-must-pass gate list, and grants the Slot one Charge. The **activation** fires in its printed window like any Slot, requires at least one Charge, and on firing spends its whole Charge Stack while its Top Card stays — the first exception to ADR 0008's full-charge cleanup, recorded as ADR 0032. Charge is **earned only**: hand cards can never be tucked here; the standing clause is the sole Charge source, and earned Charges bank across Rounds to `max_charge` with overcap wasted. The whole object is authored in `data/` — a card with `fixed: true` and a `standing` array; no part of a Signature stays in TypeScript. First increment: Elian's Riposte Ready migrates whole — the Riposte Ready Counter retires (the Signature's Charge Stack *is* the riposte count), `Shield Slam` retires (`Iron Guard` ×6→×8 fills its slots), and D-015 retires superseded. Full settled shape, twelve designer-approved sub-decisions, and rejected alternatives in [fixed-hero-power.md](design-proposals/fixed-hero-power.md). | Elian already has a fixed hero power; it lives in the rules engine rather than in the game. `createRiposteReady()` hard-codes his constants in TypeScript and the grant predicate is a branch in `resolve.ts` (D-033), so the one thing that most defines how Elian plays is invisible on the bar, unauthorable in `data/`, and needs a fresh code branch per future Hero — Kessa's Momentum next. Downstream, the deck being the only capability source means each 2-of is in hand on ~37% of Rounds and 43% of runs carry a 4+ Round drought an authored demand then charges for. Earned charge answers the battery risk by construction — hand cards physically cannot flow into the Signature, so the normal Slots keep their fuel and ADR 0002's Slot Tension — and bounds the engine's rate structurally to the authored Tank Hit cadence, the kind of bound the D-016 research says holds where numeric ones erode. D-033's stay-in-engine premise (two graded spenders) dissolves: one payoff route, no Counter, nothing graded remains to author. | The migration increment and its evaluation cohort under D-016 — any solo Boss kill is a red-flag finding, the `7`-damage full-bank ceiling is the number to watch, the engine test (a well-drawn hand still wants the Signature), and battery verification (normal Slots' fire rate unchanged). **One clause held open by the gate itself:** the banking-vs-cashing prototype (`web/prototypes/signature-banking.mjs`) shows cash-at-one-Charge strictly dominating the bank line by +30–40% damage under decision 10's numbers (`3` base `+2`/Charge), because the repeated base prices firing frequency and the earn resolves before the same Round's Quick Window, making overcap avoidance trivial. **Ruled the same day (proposal decision 13): R3, the full-bank rider** — an activation fired at `max_charge` also places `Sundered` on the Boss after its own damage resolves, keeping decision 10's numbers; the D-034 status vocabulary's first consumer, and the "class-specific full-charge mechanic" ADR 0008 anticipated. **Shipped the same day, cohort run:** zero solo victories before or after; the ADR 0027 wall holds and widens (488→507); the solo-ceiling search ends at Boss HP `2`, the pre-migration margin; offense lines within noise; blocking lines convert at last (turtle `0`→`2–4.2` Boss damage — D-015's evaporating proc cured and visible); the `banker` policy lands ~`0.2` Sundered/run. One sub-decision falsified by its own cohort and corrected in the bible: decision 11's earn mechanism — the Instant-row Tank Hit resolves before the Quick Window, so `Fortify`'s banked Armor, not `Iron Guard`'s same-Round Armor, is what produces the zero-loss block. |
| 2026-08-19 | D-065 | Adopted | The Signature leaves the Action Bar for presentation and the primary Hero gains the **Hero Frame** (ADR 0033): persistent chrome floating over the board's bottom edge, absorbing the hero branch of the Stat Panel whole — name, health with the Armor overlay, Counter chips, the deck gauge — laid out as a left-justified vertical stack — name, health with Armor, the class resource as a thin bar directly beneath it where an MMO puts mana, then the deck and discard counts. The resource bar carries no label: its position under the health bar names it, and the button that arrives when it fills names the power. The whole frame is one hold target rather than a column of them, because every button owes a 44px tap target and four interactive rows would not fit the 95 points the board leaves below its last hex row; Counter chips stay separate controls beside the frame, where an MMO puts buffs. An authored `resource_title` (Elian's is Ripostes) survives for the Detail Popup and the screen-reader label. The **Signature button** is a separate plate beside the frame, on screen if and only if the Signature can be fired right now: it arrives when the fixed Slot's window opens on an earned Charge and leaves when that Charge is spent or the window turns, so a button the player can see is always one they can press. The frame is read and the button is pressed — a split that reverses this session's own grilling answer (one object that was both meter and control), rejected on sight once built because an ability button wedged into a readout left neither shape legible at 390 points; what that answer protected survives, since the bar still makes the resource permanently visible and still teaches the earn — which is also the dependency that makes appear-when-usable honest, and the reason the same shape was refused before the bar existed. One consequence corrected in canon: a control that only exists while it is legal is not a persistent button, so ADR 0033's supersession of ADR 0006's persistent-button clause is **withdrawn** — the clause stands, and the HUD's persistent buttons remain phase progression and recovery alone. The rules do not move: the Signature stays the fixed Slot D-064 shipped, and the frame is where that Slot renders. The bar returns to two Slots at `2 \| 4 \| 4 \| 2`, ADR 0002's progression reservation returns to the third position, tapping the Hero's tile pulses the frame instead of opening a panel, the Stat Panel becomes Enemy-only, the frame is the notification dock's floor, and the catalog refuses a targeted `fixed` card until a Hero needs one. A granted Charge ignites its pip with paired Board Feedback at the Hero's hex; a wasted earn floats in a lost tone. Built as the party-frame seed, solo implementation. | Three narrow plates over-packed the 390-point ladder — both card titles truncated — and the Signature is not a Slot a hand can touch: its Charge is earned, so direct manipulation has nothing to drag, and its bank-versus-cash read (D-064 decision 13) needs the pips visible through the windows the Signature cannot act in — a readout that is sometimes closed cannot carry a resource. This supersedes ADR 0006's persistent-button clause (now: phase progression, recovery, and the Signature control) and resolves the interface direction's named Stat-Panel-versus-token-state tension on the Hero's side only; the Enemy keeps the tap panel, making own-state-versus-Enemy-state spatial rather than temporal. The board never resizes: the width-bound canvas now pins to its container's top so the spare height pools under the last hex row, where the frame and the dock's prompts live. | 373 tests (Signature-control face machine, hero-tap pulse, earn/waste Board Feedback, targeted-fixed and resource_title refusals), mutation audit 37/37, and the full browser smoke: teaching slice shows the frame without a control, the Ashen Trial shows it with one, the dock stacks off the frame without covering a hex, hero taps open no panel, ladder back on one unit at `2 \| 4 \| 4 \| 2`, contrast and 44px sweeps green, record replay byte-identical. No engine change — the sweep and Scenarios are untouched. |
| 2026-08-19 | D-066 | Adopted | A Hero is first-class authored content (ADR 0034): `data/heroes/<id>.json` carries identity, `max_health`, and the printed `signature_card`, and an Encounter fields a Hero by id (`hero`, with `fields_signature: false` to hold the Signature back, as the teaching slice does). The Encounter's inline `primary_hero_id`/`primary_hero_title`/`player_health`/`signature_card` are gone; the Hero definition is reachable content, so retuning it starts a new evidence cohort. Authoring templates live in `docs/content/templates/` (schema-pinned by `templates.test.ts`), `npm run scaffold -- <type> <id>` stamps them into `data/`, and [authoring-a-new-hero.md](authoring-a-new-hero.md) walks a new Hero end to end. | The design-team handoff could not say "create a character": a Hero could not exist before an Encounter, could not be shared by two fights without hand-kept copies (Elian's 34 health was restated in both Embermaw encounters with nothing checking agreement), and the daily workflow's "copy the closest file" made every new file start as Elian's or Embermaw's. | Existing suite green with the two encounters migrated; committed Scenarios replay byte-identically (`guardian` id kept); a scaffolded Hero + Encounter reaches the Workbench with no engine edit. |
