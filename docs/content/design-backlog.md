# Design Backlog

Status labels: `Now` means content/design work can begin after its stated proof; `Engineering` means a rules or UI extension is required; `Later` is intentionally held.

## Now

| Rank | Item | Owner | Blocking proof | Exit condition |
| --- | --- | --- | --- | --- |
| 1 | Establish deck-evaluation scorecard and measurement plan | Design/QA | [deck-evaluation-rubric.md](deck-evaluation-rubric.md) and [deck-evaluation-measurement-plan.md](../artifacts/deck-evaluation-measurement-plan.md) reviewed | Viability and Play-feel scores can be collected for the current Elian Voss deck before live deck expansion. |
| 2 | Add a Whelp-clearing card to the test deck | Design | `whelp_clear` probe | A new player can identify, select, and remove a Whelp. Also unblocks Escalation acceleration: Embermaw's Brood Call penalty is authored at `0` until this exists (D-003, ADR 0027). |
| 3 | Add a Slow Top Card to the test deck | Design | `slow_window_card` probe | Slow contains a meaningful, legal activation. |
| 4 | Run the first-three-Rounds teaching test | Design | ordered timeline probe plus three observed runs | Each Round teaches one answer before combination pressure. |
| 5 | Reconcile mobile labels and capture a fresh portrait baseline | Design/UI | mobile HUD capture | No visible `Tempo` meter or stale terminology remains. |

## Engineering

| Rank | Gap | Why it blocks content | Minimum extension |
| --- | --- | --- | --- |
| 1 | Deck-evaluation reporting facts | Deck quality cannot be compared repeatably without viability and play-feel evidence. | Scenario/run label, report aggregation for viability basics, and per-Round hand/Slot summaries listed in [deck-evaluation-measurement-plan.md](../artifacts/deck-evaluation-measurement-plan.md). |
| 2 | ~~Minion end-of-Round intent~~ Shipped 2026-08-17 (D-006): Whelps advance and bite at the end step; `minionIntents` is the visible projection awaiting board UI wiring. | Whelps cannot pressure or advance, so `Kill Adds` has no deadline. | Authored Minion behavior, deterministic move/attack resolver, visible intent projection. |
| 3 | Phase triggers and program swaps | Embermaw cannot enter the approved Conflagration package. | Encounter phase condition, post-Round transition, program selection, phase reveal UI. |
| 4 | Delayed markers and board-origin patterns | Ashen Brand and Cinderstorm cannot be represented honestly. | Marker state, delayed resolution trigger, origin/pattern schema, board overlay. |
| 5 | Rear arcs, displacement, and collision | Molten Tail and meaningful flanking are unavailable. | Rear-arc target query, forced movement action, deterministic collision policy. |
| 6 | ~~Multi-Hero party model — the engine and content seams~~ **Shipped 2026-08-19** (D-069, ADR 0035): an Encounter fields an ordered `party` of one to four seats, each with a Hero, a start, an optional per-seat deck and `fields_signature`; a Beat's `target_selector` now picks the Hero it lands on with seat-order ties and a recorded fallback; `ally` joins the target families so a card's Armor, healing, and Counter reach a chosen party member. **The zero-health lifecycle shipped 2026-08-20** (D-070, ADR 0036) and was **collapsed to one state the same day** (D-074, ADR 0040): the window, `Incapacitated`, `expiredRescues`, `incapacitateHero` and the `unanswered_rescue` charge are all removed. What stands is `Downed` — stable, derived from `health <= 0`, a body that answers no demand, with Slots and Charges frozen and no hand refill — plus `Revive` at an authored absolute `revive_to`, the three diminished actions re-homed onto Downed at one card each, and defeat as the whole Party Downed at once. Remaining: **committed intents**, **Threat**, **party HUD**, and **party-aware evaluation policies**. | Role-targeted raid content had no playable validation path, and a Healer's core loop was unexpressible: healing landed only on the firing Hero and a piece target had to be an Enemy. | 438 pre-existing tests unchanged plus a 10-test party suite; three mutation entries, one of which survived on first run and was answered with the missing test. |
| 7 | ~~Escalation as the single clock~~ Shipped 2026-08-17 (D-023, ADR 0027): counted `0`–`5` value, ticks from `Encounter Clock - 4`, authored per-Beat acceleration, four threshold effects, top threshold ends the fight. The HUD gauge shipped 2026-08-17: five pips in the program strip's header, filled bands in coral, the wipe band ringed, and every band named in its Detail Popup. Remaining: raising Embermaw's Brood Call penalty above `0` once a Whelp answer exists. | The stalemate wall rested on a health value that better play erodes, and Damage owned nothing structural. | Sweep and Scenarios regenerated; `checkpoint%` unchanged on all twelve prior policies; the new `turtle` policy measures the enrage wall. |
| 8 | ~~Forecast Row and staged Beat disclosure~~ Shipped 2026-08-17 (D-021, ADR 0026), **removed 2026-08-18 (D-053, ADR 0031)**. `forecast()`, the third row, and its popup are deleted; `consequence_tier` is kept and now gates where a `Severe` Beat may *open* rather than which horizon it must reach. | The row was built because the `Severe` tier had nowhere to appear. It was removed because it failed its own acceptance gate: across 200 seeds per position a policy reading the row and one blind to it finished in the same Round to two decimal places. | The tier is exercised now — `Brood Call` and `Within Reach` are both `Severe` on the Escalation clause — and the rule it feeds is that no phase's first program may carry one. |
| 9 | Commitments (D-028, D-035) | Nothing can bind to a named Beat, so D-027's Timeline conversion rate stays an observer's judgement instead of a count. Fortify is not a Commitment (retracted), so the concept has no implementing content. | Beat instance ids minted at Timeline entry, a card effect bound to one instance and resolving with it, visible to the Party. The ADR moment arrives with this work. Deferred deliberately while it has no consumer. |
| 10 | Promote a Counter-reading card into the live deck (D-034, D-052) | Quench exists, is measured, and sits in the `aegis_heat_answer` evaluation deck rather than the starter list: the automatic evidence shows the swap costing 22% of Boss damage to plans that do not build around it, and default-deck promotion needs the human half of the rubric. Sundered and Weakened remain authored and unplaced. | The human rubric half — class fantasy, perceived meaningful choice, fatigue — scored against the candidate deck. `npm run evaluate -- --deck aegis_heat_answer` produces the automatic side on demand. |
| 11 | Encounter Responsibilities (D-029) | Repeat clears have no social variation, and decision concentration has no structural lever beyond demand density. | An assignable transferable duty independent of Archetype, plus its handoff action. Needs the Party model. |
| 12 | Module Slot and Raid Seed (D-024) | Without bounded variation a returning player has learned the whole Boss after a few attempts, and attempts cannot be compared across groups. | One authored slot per Boss Program, seed-driven module selection settled at setup, and a printed seed value. Selection must obey D-022. |
| 13 | ~~Whether permanently Scorched ground drops the Counters on it~~ — **answered by D-050**: it does. Temporary Hazards do not. | — | — |
| 14 | ~~The Signature Slot — a Hero's engine given a visible, authorable home~~ **Shipped 2026-08-19** (D-064, ADR 0032; [proposal](design-proposals/fixed-hero-power.md) carries the full record). Elian's *Riposte* is live: a fixed third Slot authored in `data/cards/elian_riposte.json`, earned-only Charge through the standing-clause Grant, the full-bank `Sundered` rider (decision 13), `Shield Slam` and the Riposte Ready Counter and D-015's graded cash-out all deleted from the engine, `Iron Guard` to 8. **D-065 (ADR 0033) moved the Signature's face to the persistent Hero Frame** — resource pips, ignition button, earn/waste Board Feedback built; the bar is back to two Slots. Remaining: the teaching slice (`embermaw_first_turn`) fields no Signature until its scripted first turn learns one, and the human play-feel pass. | A Hero's engine had nowhere to live: Riposte Ready was hard-coded in `counters.ts`/`resolve.ts` (D-033), invisible on the bar, unauthorable in `data/`, needing a code branch per Hero; and the deck being the only capability source meant ~37%-availability 2-ofs and 4+-Round droughts Escalation charged for. | Cohort run 2026-08-19 (30 seeds × 42 policies + the regenerated Scenario suite): **zero solo victories**; the ADR 0027 enrage wall holds and widens (488→507 qualifying runs); Round-4 checkpoint still discriminates (28/14); the solo-ceiling search ends at Boss HP `2` — the same margin as before the migration. Offense lines are within noise of the old rule; the blocking lines finally convert (turtle `0`→`2–4.2` Boss damage — D-015's evaporating proc, cured and visible). The new `banker` policy measures the full-bank line: ~`0.2` Sundered/run. One claim falsified and corrected in the bible: `Iron Guard`'s Quick-window Armor cannot block the Instant-row Tank Hit, so `Fortify`'s banked Armor is the earn's real fuel. |
| 15 | ~~Signature earn vocabulary is Warden-only~~ **Shipped 2026-08-20** (D-071, ADR 0037): all four `when` values evaluated, gates paired to their event, `effect_landed` added as the first non-tank gate ([request](design-proposals/signature-earn-vocabulary.md)) | Only `host_takes_damage` is evaluated, and both gates (`health_loss_zero`, `guarded_front`) are tank concepts, so every Signature the game can print is earned by being hit. D-064 decision 3 promised a Signature fully authorable in `data/`; that promise holds for a Warden and for nobody else. A new Hero can be authored today except for the one field the Character Design Bible makes their identity. | Evaluate `host_deals_damage`, `slot_fired`, and `round_start` at the resolution points that already fire them (`resolve.ts:853`, `:766`, `:530`), each added to `EVALUATED_GRANT_WHENS` only as it is genuinely read, plus one non-tank gate to prove the list can hold one. Elian's cohort must be unchanged. |
| 16 | The Healer's half of D-074 (ADR 0040) | Rescue is designed as superiority, not exclusivity, and only the universal half is built. The Healer's ranged, repeatable Signature Revive has nowhere to live: `data/keywords/` holds one `role` keyword and it is `tank`, `data/heroes/` holds one Hero and it is a Guardian, and D-071's four `when` values are all host-facing, so an earn that fires when an *ally* is hit is a fifth with its own `GATES_BY_WHEN` row. The `rescue` `answer` Keyword waits with them: no card carries it yet, and a keyword nothing references is dead content that reads as canon. **One collision to resolve, not discover**: the Revive and the researched **Bond** both claim the Healer's one Signature Slot ([issue 17](../../.scratch/product-backlog/issues/17-first-healer-hero.md), already `ready-for-agent`). What D-074 fixes is only that a guaranteed Revive is guaranteed by a fixed Slot rather than by a deck; which card the Enchanter prints in hers belongs to the change that authors her. | A `healer` Role Keyword, the Hero and their deck, the ally-facing Grant `when` with its gate row, the `rescue` `answer` Keyword, and a ruling on the Signature Slot collision. |

## Later

- Kessa Varn, the Vanguard second tank ([design](heroes/kessa-varn-design.md), D-014): held until her required engine seams exist — Momentum resource, card-granted movement, printed activation costs, Boss facing manipulation with a `Braced` guard, and the [Signature earn vocabulary](design-proposals/signature-earn-vocabulary.md) her Momentum earn needs (Engineering rank 15). Her Hero file and identity are authorable today; her deck is not, and her own design doc forbids a proxy built from existing fields. Her `Breach` party window additionally waits on the multi-Hero model.
- Class-resource and tank Threat content after multi-Hero targeting exists.
- Raid-run rewards, branching nodes, and deck evolution after a single encounter has a proven card curve.
- Per-card art and final VFX after interaction and pattern readability pass mobile tests.
- Archive/Echo (D-030): a bounded per-Boss option held until a second Boss exists whose identity is memory. Embermaw does not take it.

## Probe Policy

Per ADR 0019, the web Encounter Engine is the rules source of truth and the Godot probe suite is frozen. Every new mechanic gets focused Vitest coverage in `web/src/engine`, and every encounter or balance change re-runs the evaluation sweep (`npm run evaluate`) and the Scenario generator (which exits red-flag on any solo victory, D-016). A content description alone is never a substitute for an executable test.
